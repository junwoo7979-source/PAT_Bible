# PAT Bible 수정 이력 (페이지별)

> 규칙: 페이지를 수정할 때마다 해당 페이지에 `-1`, `-2` … 버전을 올려 기록한다.
> 예) `로그인페이지 -1`, `관리자 -1`, `관리자 -2` …

> ⚠️ **동결(FREEZE) 2026-06-22**: 아래 함수들은 정상 동작 확인됨 — **임의 변경 금지**(변경 시 오류).
> 명시적 요청 시에만 수정.
> - 백엔드: `getDashboard`, `saveConfig`, `aggregate.js`(`countParishMembers`/`countCompletedMembersByParish`/`normalizeParish`)
> - 프론트: `saveAdminParishData`, `loadAdminParishData`, `applyAggregatedToAdmin`, `renderAdminParishFromStorage`, `updateParishPct`, `renderParishStatsFromAggregated`, `computeAggregatedData`, `applyCloudConfig`, `formatWeekPeriod`, `updateWeekFromDate`

---

## 2026-06-27

### 교구/목장 설정 -1 (교회별 그룹 동적화 — 멀티교회 핵심)
- **목적:** 하드코딩된 세광 교구(1·2·3교구+블레싱)를 → **교회별로 호칭(교구/목장/구역/그룹)·그룹 이름·개수를 자유 설정**.
- **백엔드(동결 함수, 하위호환):** `aggregate.js` countParishMembers/countCompletedMembersByParish에 `groups` 인자 추가(없으면 1·2·3교구 레거시 = 세광 회귀 안전). 정확일치→normalize 폴백으로 옛 자유입력도 매칭. `getDashboard`가 `parishConfig.groups` 읽어 동적 집계 + 응답 포함. `saveConfig`가 `parishConfig` 저장.
- **프론트:** `getParishConfig()`(없으면 기본 교구4 폴백) · 관리자 "🧩 교구/목장 설정" UI(호칭+그룹 추가/수정/삭제) · 교구별 현황표 **동적 렌더**(p1/p2/p3 하드코딩 대체, 그룹키 parishTotals) · 가족 등록 교구 **드롭다운** · 성도 대시보드 동적. 블레싱=일반 그룹(명 단위)으로 통일.
- **세광 마이그레이션:** config에 `parishConfig=교구[1·2·3교구,블레싱]` + parishTotals 그룹키 변환(150/120/110/30).
- **검증:** 단위테스트(레거시=기존동일/동적/커스텀) · Playwright(세광 교구4·커스텀 목장3·드롭다운·그룹키저장·렌더 1/10=10%) · 백엔드(목장교회 새싹3) ✅
- 파일: `functions/aggregate.js`, `functions/index.js`, `app/index.html`, `app/js/{app-core,memorize,family}.js`, `app/firebase-db.js`

### 교회 정보 관리 -1 (앱제목 자동화 + 담임목사·주소)
- "앱 상단 제목" 입력/버튼 삭제 → **교회 이름 저장 시 앱 제목 자동 동기화**(로컬+서버 appTitle). **담임목사·주소** 입력란 추가(교회 정보 저장 버튼). 로그인 "새 교회 등록"→"교회 등록".
- 파일: `app/index.html`, `app/js/{verse,app-core}.js`

## 2026-06-26

### 로그인/시작 -2 (타 교회 설치 시 세광 브랜딩 제거)
- **증상:** 옛 버전을 띄운 적 있는 기기는 `pat_app_title='세광교회'`가 캐시로 남아, 중립시작 배포 후에도 로그인 첫 화면에 "세광교회"가 떠서 타 교회에 이질감.
- **수정:** `applyAppTitle()`이 **교회 미선택(`DB.church.code` 빈 값)일 땐 옛 캐시 무시하고 무조건 "PAT Bible"**. 교회 선택 후에만 그 교회 제목.
- **참고:** 앱 아이콘·이름(manifest)은 이미 "PAT Bible"(중립). 앱이 하나라 아이콘은 교회별 분리 불가.
- **검증(Playwright):** pat_app_title='세광교회' 캐시 + 교회 미선택 → "PAT Bible" / 교회 선택 → 그 교회명. 
- **배포:** **Firebase Hosting(pat-bible-app.web.app)** + GitHub Pages. sw.js v72→v73.
- 파일: `app/js/app-core.js`, `app/sw.js`

### 문의·오류 신고 -1 (접수 창구 + 자동 오류 수집)
- **목적:** 교회가 어디로 연락할지 고민 없이 앱에서 바로 신고 → 개발자가 한눈에 확인(접수 자동화).
- **백엔드:** `submitReport`(공개), `getReports`/`deleteReport`(개발자 토큰). `reports` 컬렉션.
- **프론트:**
  · 신고 화면 `s-report`(현재 화면·교회 자동 첨부) + 진입 버튼 3곳: 로그인 / 교인 홈 메뉴(hm-dropdown) / 관리자 헤더.
  · **자동 오류 수집:** `window.onerror`·`unhandledrejection` → submitReport(type:auto). 같은 메시지 1회·세션당 8건 상한(폭주 방지).
  · `firebase-db.js`: submitReport/getReports/deleteReport 래퍼.
  · **개발자 통계(dev-stats.html):** "📨 신고·오류" 목록(문의/자동오류 배지 · 교회 · 화면 · 시간 · 메시지 + ✓삭제).
- **검증(Playwright/curl):** 수동 신고 전송→적재, 자동수집 중복·상한 동작, dev-stats 목록 렌더(3건·배지), 삭제. 테스트 신고 정리 완료.
- **배포:** sw.js CACHE_NAME v71→v72.
- 파일: `functions/index.js`, `app/firebase-db.js`, `app/js/app-core.js`, `app/index.html`, `app/dev-stats.html`, `app/sw.js`

### 비밀번호 규칙 -1 (관리자 비번 = 특수문자→영문→숫자 순서, 8자+)
- **변경:** 관리자 비밀번호 규칙을 "숫자 8자리" → **"특수문자 → 영문 → 숫자 순서, 8자 이상"**(예: `#grace2026`)으로.
- **안내 문구:** 교회 등록 화면 비번 필드 아래 "🔒 특수문자 → 영문 → 숫자 순서로 8자 이상 (예: #grace2026)" 상시 표시 + placeholder.
- **검증:** 프론트(`registerChurchSubmit`)·백엔드(`ADMIN_PW_RE`) 동일 정규식 `^(?=.{8,}$)[^A-Za-z0-9]+[A-Za-z]+[0-9]+$`.
- **테스트(curl):** #grace2026 → 200 / 숫자만 → 400 / 순서틀림(grace#123) → 400 / 8자미만 → 400.
- 파일: `functions/index.js`, `app/index.html`, `app/js/app-core.js`, `app/sw.js`(v71)
- ※ 가족 비번/재설정 등 다른 비번 필드 적용 및 "개발자 사용자 확인" 범위는 추가 협의.

### 로그인/시작 -1 (중립 시작 — 멀티교회 SaaS)
- **변경:** 신규 방문자가 앱을 처음 열면 세광교회가 자동 표시되던 것을 → **교회 미선택(중립 "PAT Bible") 화면**으로. 교회는 코드 입력/관리자 로그인으로 선택.
- **구현(`app/js/app-core.js`):**
  · `DB.church` 기본값을 중립(`code:'', name:''`)으로.
  · `applyStoredData`: pat_church_code 있으면 그 교회, 없지만 가족/관리자 데이터 있으면(멀티교회 이전 기존 사용자) **세광 11111 폴백**(회귀 방지), 그 외 신규 방문자는 중립.
  · `initFirebase`: 교회 미선택 시 설정 로드 보류(세광 자동표시 방지). 설정 로드를 `loadChurchConfig()`로 추출.
  · `enterChurch`: 입력 코드를 서버 조회해 등록된 교회면 전환(세광 11111 포함). 구절 미등록 교회는 구절 비움.
  · `adminLogin`(세광/신규 모두): 로그인 후 해당 교회 설정 로드.
- **세광교회 유지:** 코드 11111 입력 또는 admin/1234 로그인 시 기존대로 세광(구절 포함). 기존 사용자는 폴백으로 그대로.
- **검증(Playwright):** 신규=중립("PAT Bible") / 11111→세광(베드로전서4장19절) / 신규교회 코드→자기 구절 / 기존 사용자 폴백 11111 / 세광 admin/1234 → 세광. 오류 0.
- **배포:** sw.js CACHE_NAME v69→v70.
- 파일: `app/js/app-core.js`, `app/sw.js`

### 개발자 통계 -1 (전 교회 사용 현황 + 교회 삭제 도구)
- **목적:** 여러 교회가 사용할 때 개발자가 "몇 개 교회·몇 명"을 한눈에 확인.
- **백엔드:** `getPlatformStats`(전 교회 집계 — `listDocuments`로 레거시 11111 포함, 교회별 가정·인원[멤버 배열∪서브컬렉션 합집합] 합산), `deleteChurch`(교회 재귀 삭제 — 테스트/스팸 정리). 둘 다 **개발자 토큰**(Firebase Secret `PAT_DEV_TOKEN`) 검증. 앱/코드에 토큰 노출 없음.
- **프론트(플랜 C = 둘 다):**
  · 별도 URL 페이지 `app/dev-stats.html` — 토큰 입력→요약(교회·가정·인원)+교회별 표+교회 삭제(확인 후).
  · 앱 내 숨김 화면 `s-dev-stats` — `?dev=1` 진입, 토큰 입력→요약+목록(`devStatsLoad`).
- **검증(curl+Playwright):** 토큰 없으면 401 / 정상 토큰 집계 / 별도 페이지·앱내 화면 렌더 / 테스트 교회 8개 삭제 후 세광 11111만 남음 확인.
- **정리:** 테스트로 생성된 교회(zz*, #100625, #901214, TEST_CHURCH, demo-church) 전부 삭제 → 현재 1교회(세광).
- 파일: `functions/index.js`(getPlatformStats, deleteChurch, PAT_DEV_TOKEN), `app/dev-stats.html`(신규), `app/index.html`, `app/js/app-core.js`, `app/sw.js`(v67)

### 기도 페이지 -1 (음성 인식 엔진을 암송과 통일)
- **변경:** 기도 음성 입력을 브라우저 Web Speech API(`SpeechRecognition`, ko-KR) → **암송과 동일한 Groq Whisper(클라우드)** 방식으로 통일.
- **이유:** 기존 Web Speech API는 카카오톡 인앱브라우저·일부 기기에서 미지원("음성 인식을 지원하지 않습니다"). Groq Whisper는 녹음 업로드 방식이라 구형폰·인앱브라우저에서도 동작.
- **구현(`app/js/prayer.js`):** `startPrayerMic`을 MediaRecorder 녹음 → `PAT_DB.transcribeAudio`(Groq) 전사 → 실패 시 `transcribeBlob`(브라우저 Whisper) 폴백 → 결과를 `prayerText`에 삽입·텍스트탭 전환. 90초 제한 타이머 유지. UI 레이아웃 동일(요소·버튼 그대로, 실시간 받아쓰기 → 종료 후 ~1~2초 인식으로 동작만 변경).
- **검증(Playwright, 합성 오디오+목):** 녹음 시작(recording, ⏹️) → 정지 → transcribeAudio 호출 → 텍스트 삽입·텍스트탭 전환 / Groq 실패 시 transcribeBlob 폴백 동작 ✅
- **배포:** `sw.js` CACHE_NAME v65→v66.
- 파일: `app/js/prayer.js`, `app/sw.js`

## 2026-06-25

### 가족방 -4 (재등록 중복 누적 근본 차단 + 기존 중복 정리)
- **증상:** 대표가 가족방을 다시 등록하면 **서버에 가정 문서가 중복 생성**(교회 등록 인원에 잔류). 11111 권호택/예운네가 134·135 등 **3개까지 누적**.
- **원인:** familyId 분실(카톡 인앱브라우저/다기기/이중제출) 상태에서 재등록하면 `saveFamily` 생성 경로가 무조건 새 문서 `add` → 포크. 서버에 중복 방지 없음.
- **수정(서버 `saveFamily`):** 생성 시 **같은 (대표+교구)** 기존 가정이 있으면 새로 만들지 않고 그 문서를 갱신(구역=최신값, 멤버=합집합). 응답 `{familyId, merged:true}` → 클라이언트가 그 id를 저장해 **familyId 자동 복구**(재포크 차단). 합의 기준: 대표+교구 동일 시 동일 가정(구역 무시).
- **신규 `deleteFamily`(관리자 전용):** family 문서 + members 서브컬렉션 + 해당 records 정리. 교회별 admin 인증(레거시 11111 전역 폴백).
- **기존 11111 정리:** 권호택 중복 3개 중 기록 보유 `Ktsr…`(예운네/135/100%) 보존, 빈 2개(`rhnd…`,`I7UX…`, 기록 0건) 삭제 → **1가정·1명**으로 정정(대시보드 registeredByParish 1교구=1, completed=1 확인).
- **검증(curl):** 격리교회에서 familyId 없이 같은 대표+교구 재등록 → `merged:true` 동일 familyId, 구역 최신화, 멤버 합집합 / 다른 교구는 별개 생성 / getFamiliesList 총 2(포크 아님). 11111 삭제 후 1가정.
- 파일: `functions/index.js`(saveFamily dedup, deleteFamily)

### 멀티 교회 -1 (교회별 독립 등록·로그인·격리)
- **목적:** 하드코딩된 단일 교회(세광/11111, admin/1234)에서 → **누구나 교회를 셀프 등록**해 독립 운영.
- **데이터 모델:** `churches/{코드}/admin/cred`{adminId, adminPwHash}, `churches/{코드}` 루트{name}. 데이터는 기존처럼 교회코드로 네임스페이스.
- **백엔드(신규/수정):**
  · `registerChurch`(오픈 셀프 등록, 기존 교회 코드 덮어쓰기 409 차단)
  · `adminLogin`(교회코드+아이디+비번 대조)
  · `checkChurchCode`(코드 중복확인)
  · `saveVerse`/`saveConfig`: 전역 토큰 → **교회별 admin 검증**(`assertChurchAdmin`). admin 문서 없는 레거시(11111)는 전역 폴백 → **세광 회귀 유지**.
  · `security.js` `validChurchCode`에 안전 특수문자(`# @ * ! - _`) 허용. **전 함수 재배포**로 모든 엔드포인트에 적용.
- **코드 형식:** 교회코드=특수문자1+숫자6(예 `#482913`), 관리자 아이디=영문3~20, 관리자 비번=숫자8.
- **프론트:** 교회 등록 화면(s-church-register, 단계 안내+중복확인), 관리자 로그인에 교회코드 필드, `DB.church.code` 동적화(`pat_church_code`), 교인 입장 시 신규형식 코드면 교회 전환(`enterChurch`), `adoptChurch`/`registerChurchSubmit`/`checkRegChurchCode`.
- **세광교회:** 테스트 기간 현행 유지(코드 비움+admin/1234 → 11111). 추후 동일 방식 전환.
- **검증(curl+Playwright):**
  · 백엔드: 세광11111+레거시토큰 saveConfig 200(회귀) / 무인증 401 / 신규 등록 ok / 신규 관리자로그인 ok / 본인 id·pw saveConfig 200 / 틀린비번 401 / **타교회 전역토큰 401(격리)** / 재등록 409 / 형식위반 400 / 코드중복확인 ok
  · 프론트: 교회 등록→자동로그인→은혜교회 진입 / 세광 관리자(코드비움+admin/1234) / 신규 관리자(코드+id+pw) / 틀린비번 거부 / 교인 코드 입장 전환
- 파일: `functions/index.js`, `functions/security.js`, `app/firebase-db.js`, `app/js/app-core.js`, `app/index.html`, `app/sw.js`(v65), `docs/guides/멀티교회-사용가이드.md`

### 홈/라우팅 -2 (로그아웃 후 새로고침 시 이전 화면 튕김 수정)
- **증상:** 로그아웃 후 페이지를 당겨서 새로고침하면 로그인 화면이 아니라 **이전 화면(가족/관리자)으로 튕김**.
- **원인:** 새로고침 시 초기화 로직은 `pat_stay_login`(sessionStorage) 플래그가 있으면 로그인 유지하는데,
  이 플래그는 `go('s-login')`을 통할 때만 설정됨. **로그아웃 함수는 `go()`를 거치지 않고 DOM을 직접 조작** →
  플래그 미설정. 게다가 `memberLogout`은 localStorage만 지우고 `loadFamilyProfile()`이 읽는
  **sessionStorage 백업은 미삭제** → 남은 자격(관리자 id/pw)·백업으로 이전 화면 복원.
  · 재현(Playwright): member 로그아웃(관리자 자격 잔존) → 새로고침 → `#s-admin`으로 튕김 확인.
- **수정:** `memberLogout`/`adminLogout` 둘 다 **`sessionStorage.setItem('pat_stay_login','1')`** 설정
  (+ `memberLogout`은 `sessionStorage` 가족 백업도 제거). 초기화 로직이 무조건 로그인 유지.
- **검증(Playwright, 실제 파일 로드):**
  · A: member 로그아웃(관리자 잔존) → 새로고침 `s-login` ✅ (수정 전 `s-admin`)
  · B: admin 로그아웃(가족 잔존) → `s-login` ✅ (수정 전 `s-family`)
  · C: 재로그인 시 `go('s-family')`가 플래그 해제 → 정상 입장(잠금 없음) ✅
  · end-to-end 실제 새로고침: 로그아웃 후 `#s-admin` 미발생, `active=s-login` ✅
- **배포:** JS 3종 변경 반영 위해 `sw.js` `CACHE_NAME` v64→v65 (안 올리면 사용자가 옛 JS 계속 사용).
- 파일: `app/js/app-core.js`(memberLogout/adminLogout), `app/sw.js`

### 가족방 -3 (구성원 삭제 동기화 — 합집합 부활 버그 수정)
- **증상:** 대표가 구성원을 삭제해도 1초 폴링 후 다시 나타남(특히 초대 링크로 실제 입장한 멤버).
- **원인:** 백엔드 `getFamilyProgress`가 **members 배열 ∪ members 서브컬렉션**(입장기록) 합집합을 반환.
  `deleteFamilyMember`는 `saveFamily`로 **배열만** 덮어쓰고 `joinFamily`가 만든 서브컬렉션 doc은 남김
  → 합집합에서 부활.
- **수정:** 삭제 시 **배열 + 서브컬렉션을 함께 정리**.
  · 백엔드 신규 `removeFamilyMember`: members 배열에서 이름 제거 + 같은 이름의 서브컬렉션 입장기록 삭제
  · 프론트 `firebase-db.js`: `removeFamilyMember` 래퍼 + export
  · 프론트 `deleteFamilyMember`(async): 새 엔드포인트 호출, **실패/미배포 시 기존 `saveFamily` 폴백**(배열만 갱신 — 회귀 방지)
- **배포 필요:** `firebase deploy --only functions`(removeFamilyMember). 미배포 시 폴백으로 기존 동작 유지(부활 버그만 잔존).
- **검증 필요(2기기):** 대표가 초대로 입장한 멤버 삭제 → 두 기기 모두에서 사라지고 폴링 후 부활 안 함.
- 파일: `functions/index.js`(removeFamilyMember), `app/firebase-db.js`, `app/js/family.js`

## 2026-06-24

### 등록가정 -1 (관리자 신규 탭) + 테스트 데이터 초기화
- **테스트 중복 가족 초기화:** 교회(11111)의 테스트 등록 **15가정·21멤버·1기록 전체 삭제** → 클린 슬레이트.
- **신규 탭 "📋 등록가정"** 추가 (시상관리 옆): **표(행열) 형식** — 컬럼 `번호·교구·구역·가족 이름(대표)·참여 인원`. 상단 요약 "총 N가정·M명".
  · 등록 즉시 교구별 현황 '참가'에 반영(동일 Firestore 소스). 5개 탭 전환 5/5 검증.
  · 백엔드 `getFamiliesList`(가족+멤버 합집합 인원)
  · 프론트 `renderFamiliesList` + `switchAdminTab('families')` + 탭 버튼/콘텐츠
  · 상단 요약: "총 N가정 · M명"
- **검증:** 탭 5개 순서(구절·교회·시상·등록가정·비번), 모의 2가정 → "총 2가정 · 5명" + 가족별 인원 표시 ✅
- 파일: `functions/index.js`(getFamiliesList), `app/firebase-db.js`, `app/js/admin.js`, `app/index.html`

### 시상관리 -1 (가족별 실천율 — Firestore 통합)
- **증상:** 시상관리가 교구별 현황과 따로 놀아 확인 불가. `getRankedFamilies`가 미완성 스텁
  (로컬 본인 가족 1개만, 존재하지 않는 `pat_rec_{날짜}` 키 사용, `profile.familyId`는 없는 필드 → 항상 빈/0).
- **수정:** 시상관리를 **교구별 현황과 동일한 Firestore 소스로 통합**.
  · 백엔드 `getAwardRanking`: 교회 전체 가족·멤버(선언+입장)·기록·총구절수로 **가족별 실천율 순위** 계산
  · 실천율 = 멤버가 완료한 구절 수 / 총 구절 수, 가족 평균. 순수함수 `rankFamilies`(aggregate.js, 단위테스트)
  · 프론트 `getRankedFamilies`/`searchFamilyForAward` → 서버 데이터 사용(UI 포맷 유지)
- **검증:**
  · 단위테스트: 김믿음가족 75% 1등 등 ✅
  · 라이브: 교회 15가족 전원 표시(실제 이름·멤버) ✅
  · **end-to-end: 테스트가족 완료 기록 → 시상에 실천율 100% 즉시 반영** ✅ (테스트데이터 정리 완료)
- 파일: `functions/index.js`, `functions/aggregate.js`(rankFamilies), `app/firebase-db.js`, `app/js/admin.js`
- 참고: 교구별 현황(완료/전체)은 관리자↔대시보드 이미 일치. 대시보드만 '참가'(등록인원) 추가 표시 — 포맷 유지로 관리자엔 미추가.

## 2026-06-23

### 가족방 -2 (등록 후 초기화 — 재발 버그 근본 수정)
- **증상:** 대표가 가족방(이름·대표·교구·구역) 등록 후 또 초기화로 돌아감.
- **원인:** `startFamilyProgressPolling`(1초 동기화)이 서버에서 **빈 값**을 받으면
  (가족 doc 누락·타이밍·일시 오류) 조건이 `!== undefined`라 **빈 값으로 로컬 등록정보를 덮어써 삭제**.
  → roomName/leaderName/parish/district가 지워져 "등록 안 됨" 상태로 보임.
- **수정:** 서버 값이 **있을 때만**(truthy) 갱신 → 빈 응답이 로컬 데이터를 지우지 못함.
- **검증:** 불완전 familyId로도 재로딩 시 "믿음가족 / 대표 김대표 1교구 3구역" 유지(로컬·라이브).
  (수정 전엔 "김대표네 가족 / 김대표"로 초기화됐음)
- 파일: `app/js/family.js`

### 가족방 -1 (오류 2건 수정)
- **Bug1: 등록만 해도 이번주 달성률 N/N(완료)로 잡힘 (발생 안 한 데이터 반영)**
  - 원인: `renderFamilyMemberList`가 완료수=인원수, 100% 하드코딩 + 멤버 전원 "✓ 완료" 표시.
    백엔드 `getFamilyProgress`도 `done:true` 고정. `renderFamily`도 `done:true` 하드코딩.
  - 수정: 완료 = **현재 구절 실제 기록**으로 판정.
    · 백엔드 `getFamilyProgress`: records(familyId+verseRef)로 멤버별 done 계산
    · `renderFamilyMemberList`: `filter(done)`로 카운트 + 완료/미완료 표시
    · `renderFamily`: 본인은 로컬 기록, 나머지는 미완료(클라우드 동기화로 보정)
    · `DB.members` 데모 기본값 done:false
- **Bug2: 대표 등록 후 가족방이 자꾸 초기화**
  - 원인: `refreshFamilyProfileByPassword`가 매 렌더마다 비번으로 가족 조회 →
    기본 비번이 교회코드로 공유 → familyId 확정 전 **다른 가족이 매칭되어 프로필 덮어씀**.
  - 수정: familyId 확정된 경우에만, 반환 가족이 내 familyId와 일치할 때만 갱신.
- 테스트: 등록직후 0/2명·미완료, 본인완료 1/2명, 초기화 차단 (6/6 통과)
- 파일: `functions/index.js`(getFamilyProgress), `app/js/family.js`, `app/js/app-core.js`

## 2026-06-22

### 홈/라우팅 -1 (중대 오류 수정)
- **증상:** 홈(성도) 화면에서 새로고침하면 관리자 화면으로 튕김.
- **원인:** `completeAppInitialization`이 가족 판정을 `savedFamily.familyId`로 했는데,
  `familyId`는 프로필 객체가 아니라 별도 키 `pat_family_id`에 저장됨 → 조건이 항상 거짓 →
  관리자 자격(`pat_admin_id/pw`)이 저장된 기기에서 가족이 있어도 `s-admin`으로 덮어씀.
- **수정:** 가족 판정을 `determineInitialScreen`과 동일하게 "프로필 존재"만으로 → `s-family` 우선.
- **테스트:** 가족+관리자이력→홈, 미동기화가족+관리자→홈, 관리자만→관리자, 초기→로그인 (4/4 통과)
- 파일: `app/js/app-core.js`

### 음성 암송 페이지 -6 (Groq 클라우드 전환 — 1~2초)
- 브라우저 Whisper(4초~폰 수십초) → **Groq Whisper large-v3-turbo(클라우드)** 전환.
  · 폰은 녹음만 업로드, 서버 GPU가 전사 → **~1~2초** (구형폰도 동일)
- 구성: 백엔드 `transcribeAudio`(Groq 프록시, 키는 Firebase Secret `GROQ_API_KEY`),
  프론트 `groq-asr.js`(`groqToggleMic`) + `GROQ_ENABLED=true`, 실패 시 Whisper 자동 폴백.
- 검증(라이브): 함수 직접 1.7초, 브라우저→Groq **1.2초**, 전사 정확(자모/포함 채점 100%).
- ⚠️ 후속: `transcribeAudio`는 현재 cors-only(무인증) — 남용 방지용 토큰 검증 추가 권장.
- 파일: `functions/index.js`, `app/firebase-db.js`, `app/js/groq-asr.js`, `app/index.html`

### 음성 암송 페이지 -5 (모델 등급 검토 → base 유지 확정)
- 상위 모델 실측(WASM·데스크톱 4스레드):
  · whisper-tiny: 2.5초지만 오인식("여호와는"→"영어하는"), 정확도 borderline 90% → 제외
  · whisper-large-v3-turbo: **728MB·로드93초·전사68초** → 사용 불가
  · large-v3(풀): turbo보다 더 큼/느림(디코더 32층) → 더 불가
- 결론: 브라우저 내장(무료·오프라인) 조건에서 **`whisper-base`가 최적** → 유지.
  (turbo급은 GPU 전용 → 원하면 클라우드 STT만 현실적: API키·비용·네트워크 필요)
- 모델 미변경(라이브 그대로 base). 코드 `WHISPER_DTYPE` 상수만 정리.

### 교구별 현황 -1 (시뮬레이션 형식 반영)
- 교구별 현황에 **참가인원** 추가 + 라벨 **진도표→실천표**, % = 실천율.
  · 백엔드 `getDashboard`가 `registeredByParish`(교구별 등록 인원) 반환
  · `renderParishStatsFromAggregated`에 참가인원 표시(`완료 N/전체 · 참가 N`)
- 파일: `functions/index.js`, `app/js/memorize.js`, `app/index.html`

### 음성 암송 페이지 -4 (대기시간 단축 — 반응 속도 개선)
- **증상:** 정확도는 나오나 전사 대기시간이 김(단일스레드 ~9초).
- **원인:** WASM이 **1스레드**로 동작(교차출처 격리 미설정 → SharedArrayBuffer 불가).
  (포트 8000을 Python 서버가 점유해 로컬 헤더 적용도 안 됐던 점도 발견)
- **수정:** **교차출처 격리(COOP/COEP) 헤더 추가** → WASM **멀티스레드(4코어)** 활성화.
  · `firebase.json`에 `Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: credentialless`
  · `server.js`(로컬)에도 동일 헤더
- **검증(라이브):** crossOriginIsolated=true, threads=4, 전사 **9초→4.1초(2.3배↑)**, 정확도 유지.
  Firebase(getConfig·getDashboard)도 COEP에서 정상 동작 확인(생산 안전).
- 참고: whisper-tiny(2.5초)도 검토했으나 정확도 borderline(90%)이라 **base 유지**.
- 파일: `firebase.json`, `server.js`, `app/js/whisper-asr.js`(멀티스레드 설정)

### 음성 암송 페이지 -3 (실기기 성능 문제 해결)
- **증상:** Whisper 교체 후에도 마이크 "성능 개선 안 됨".
- **직접 테스트로 찾은 원인 3가지:**
  1. **WebGPU 추론이 행(hang)** — 모델은 로드되나 전사가 무한 대기 → 결과 없음.
     → **WASM(안정)으로 전환**(WebGPU 미사용). WASM 멀티스레드 설정(교차격리 없으면 1스레드).
  2. **whisper-small이 너무 느림(40초+)** → **whisper-base로 교체**(전사 ~11초).
  3. **채점이 여분/반복 발화에 취약** — Whisper가 침묵 구간에 문장을 반복 생성하면
     edit-distance가 길이차로 급락(예: 23%).
     → **포함(containment) 채점 추가**(`containmentSimilarity`): 구절이 전사 안에 들어있으면
     앞/뒤 여분·반복을 무시하고 ~100% 인정. 반복 억제 `no_repeat_ngram_size`도 추가.
- **검증:** 가짜오디오 전체 흐름(녹음→Whisper→채점) — 전사 정확, 올바른 구절 대비 **유사도 100%**.
  (테스트 중 23%는 config 폴링이 DB.verse를 라이브 구절로 덮어쓴 하네스 artifact로 확인)
- 파일: `app/js/whisper-asr.js`(base·WASM·반복억제), `app/js/voice.js`(containmentSimilarity), `app/js/voice-ui.js`(채점)

### 음성 암송 페이지 -2
- **정확도 향상 → 유사도 100% 달성**
- ① 채점에 **자모(음소) 단위 비교** 추가(`jamoNormalize`): "목자시니"↔"목자신이"처럼
  ASR 재음절화·무음 초성 ㅇ 차이를 음소 레벨에서 동일하게 인정 (90%→**100%**)
- ② Whisper 모델 `whisper-base` → **`whisper-small`** (한국어 정확도↑)
- 테스트: TTS 한국어 음성 → 자모 유사도 **100%** 확인(헤드리스)
- 파일: `app/js/voice.js`(jamoNormalize), `app/js/voice-ui.js`(previewVoice 채점), `app/js/whisper-asr.js`(모델)
- 참고: small은 최초 다운로드가 큼(약 70초, 이후 캐시). 구형폰 부담 크면 `whisper-asr.js`의 `WHISPER_MODEL`을 base로 한 줄 변경

### 음성 암송 페이지 -1
- 마이크 음성인식을 **Web SpeechRecognition → 브라우저 내장 Whisper(transformers.js)** 로 교체
  (한국어 인식 오류 감소 목적). 모델 `Xenova/whisper-base`(q8), 최초 1회 다운로드 후 캐시
- 흐름: 녹음(MediaRecorder) → Whisper 변환 → 기존 채점(`evalVoice`) 재사용
- 마이크 버튼 `toggleMic()` → `whisperToggleMic()`, 음성 화면 진입 시 모델 프리로드
- 테스트: 한국어 TTS 음성 전사 글자 90% 일치 확인 (헤드리스)
- 파일: `app/js/whisper-asr.js`(신규), `app/index.html`
- 참고: 기존 `voice.js`의 SpeechRecognition 엔진 코드는 미사용(데드)으로 남김(헬퍼는 재사용)

### 관리자 -1
- 🧭 교구별 현황 **관리 편집 폼** 추가: 완료=자동 집계(읽기전용), 전체 인원=관리자 입력
- 저장/복원 일원화(중복·죽은 코드 제거), 관리자↔대시보드 동기화 수정
- **기준 날짜 → 적용 주차·기간 자동 연동** 버그 수정(저장된 옛 주차로 덮어쓰지 않음)
- 전체 인원을 **서버(config.parishTotals)에도 저장** → 전 성도 기기와 동기화
- 파일: `app/index.html`, `app/js/app-core.js`

### 현황 대시보드(성도) -1
- 교구별 현황 = **현재 구절 진도(완료 멤버 수)** 로 표시 (진도 없으면 0 — 가짜 숫자 제거)
- 분모(전체 인원)는 관리자 설정값을 서버에서 받아 동기화
- 변경 감지에 관리자 인원 포함 → 관리자 변경 즉시 반영
- 파일: `app/js/memorize.js`

### 가족방 등록 페이지 -1
- 교구 입력을 **자유 텍스트 → 드롭다운(1교구/2교구/3교구)** 으로 변경
  (옛 자유입력 "1", "1 교구" 등이 집계 누락되던 문제 해결)
- 파일: `app/index.html`

### 백엔드(Functions) -1
- `getDashboard`: 교구별 = **완료한 멤버 수** 집계(멤버 단위 중복제거) + 교구값 정규화(레거시 "1"→"1교구")
- `getDashboard`: 등록 인원 헤드카운트(`totalMembers`) 추가
- `saveConfig`: **부분 업데이트** 지원(교구 인원만 저장 시 구절/제목 보존), `parishTotals` 저장
- 파일: `functions/index.js`, `functions/aggregate.js`(신규)

### 공통/DB -1
- `firebase-db.saveConfig(...)`에 `parishTotals` 전달
- `applyCloudConfig`: 서버 `parishTotals`를 모든 기기 localStorage로 미러링(단일 진실 소스)
- 파일: `app/firebase-db.js`, `app/js/app-core.js`

---

_배포: Firebase Functions + Hosting (https://pat-bible-app.web.app)_

### 음성 암송 페이지 -7 (통과 기준 85%)
- 일반 음성 통과 기준 90% → **85%** (`TH().voice`). 표시 "통과 기준 N%"도 자동 반영. 관대모드 80% 유지.
- 폼/레이아웃 변경 없음. 파일: `app/js/app-core.js`

### 관리자 교구별 현황 관리 -2 (디자인 정렬)
- 명시적 요청으로 UI만 개선: 교구별 카드를 stat 카드 형태로 정렬
  (큰 % + 완료 박스 / 전체 인원 입력 박스, 블레싱은 accent 상단선).
- 입력 ID·`updateParishPct`·`saveAdminParishData` 등 기능/JS 전부 보존(동결 함수 미변경).
- 검증: ID 7개 존재, 완료 자동집계 30·pct 20%·저장 정상 + 스크린샷 확인.
- 파일: `app/index.html`

### 관리자 교구별 현황 관리 -3 (등록가정 표 형식)
- 요청대로 교구별 현황 UI를 **등록가정과 동일한 격자 테이블**로 교체.
  컬럼: `교구 · 완료 · 전체 인원(입력) · 실천율`, 셀 테두리, 블레싱 행 accent 상단선.
- 입력 ID·`updateParishPct`·`saveAdminParishData` 전부 보존(동결 함수 미변경).
- 검증: ID 7개·완료 자동집계 30·pct 20%·저장 150 정상 + 스크린샷 확인.
- 파일: `app/index.html`