# PAT Bible 수정 이력 (페이지별)

> 규칙: 페이지를 수정할 때마다 해당 페이지에 `-1`, `-2` … 버전을 올려 기록한다.
> 예) `로그인페이지 -1`, `관리자 -1`, `관리자 -2` …

> ⚠️ **동결(FREEZE) 2026-06-22**: 아래 함수들은 정상 동작 확인됨 — **임의 변경 금지**(변경 시 오류).
> 명시적 요청 시에만 수정.
> - 백엔드: `getDashboard`, `saveConfig`, `aggregate.js`(`countParishMembers`/`countCompletedMembersByParish`/`normalizeParish`)
> - 프론트: `saveAdminParishData`, `loadAdminParishData`, `applyAggregatedToAdmin`, `renderAdminParishFromStorage`, `updateParishPct`, `renderParishStatsFromAggregated`, `computeAggregatedData`, `applyCloudConfig`, `formatWeekPeriod`, `updateWeekFromDate`

---

## 2026-07-17

> ⚠️ 앱 코드(JS/CSS/HTML) 변경 없음 → **캐시 버전 올리지 않음**(sw.js 미변경). 배포 불필요.
> 문서만 수정.

### 📄 문서 -1 — 문서 13곳이 가리키던 옛 Desktop 경로를 실재 경로로 수정

- **배경**: 2026-07-16에 CLAUDE.md 2줄만 고쳤으나, `docs/` 하위 문서 다수가 여전히
  `C:\Users\SAMSUNG\Desktop\ai\PAT_Bible` 을 지시하고 있었음. 세션이 옛 폴더에서 열리던
  사고와 같은 뿌리(문서가 옛 경로를 가리키면 습관이 재발함).
- **수정**(11개 파일 13건):
  - `cd`·저장소·앱 위치 → `C:\projects\PAT_Bible`
    (`docs/WORKFLOW.md` 2건, `docs/archive/작업핸드오프.md`, `docs/planning/1차_진행표.md`,
     `docs/archive/2026-06-05-mobile-voice-save.md`, `docs/superpowers/specs/2026-06-05-google-toss-shared-storage-design.md`)
  - 종합문서 링크 `Desktop\ai\pat-1단계-기획설계-실행문서.md`(**실재하지 않음**) → `_assets\docs\...`
    (`docs/archive/` 구현계획 4건)
  - **TWA 빌드 경로** `Desktop\ai\PAT_Bible_TWA`(**폴더 없어짐**) → `_assets\twa`
    (`docs/CLAUDE_CODE_인수인계.md`, `docs/실행내역서.md` APK 줄 정리)
  - Toss 앱 위치 → `C:\projects\toss-granite\src\App.tsx`
  - `docs/re/pat 개선.md` 자기 위치 표기 갱신
- **일부러 남긴 것**: `수정이력.md`의 사고 기록 3건, `docs/CLAUDE_CODE_인수인계.md:7` 금지문
  ("예전/잔재 폴더에서 작업하지 말 것") — 옛 경로가 나오는 게 정상인 줄들.
- **부수 확인**: ★TWA 키스토어 유실 아님 — 문서가 가리키던 `Desktop\ai\PAT_Bible_TWA` 는 없지만
  실물은 `_assets\twa\android.keystore`(2026-06-10) 에 보존돼 있음. (없으면 Play 스토어 업데이트 불가)
- **검증**: 새 경로 7개 실물 존재 확인(app/index.html, _assets/twa/gradlew, _assets/docs/..., toss-granite/src/App.tsx 등).
  잔여 옛 경로는 위 "일부러 남긴 것" 4건뿐.
- **작업 메모**: Git Bash `sed` 는 `\\` 가 리터럴 백슬래시로 매칭되지 않아 **오류 없이 0건 치환**됨
  (성공한 것처럼 끝남). 바이트 단위 치환으로 재작업. 앞으로 Windows 경로 치환 시 sed 신뢰 금지.

---

## 2026-07-16

> ⚠️ 앱 코드(JS/CSS/HTML) 변경 없음 → **캐시 버전 올리지 않음**(sw.js 미변경). 배포 불필요.
> 작업 환경·문서만 수정.

### 🛠 작업환경 -1 — 세션이 Desktop 옛 경로에서 열리던 문제 해결 (지침이 안 읽히고 있었음)
- **증상**: 정식 지침(`C:\projects\PAT_Bible\CLAUDE.md`, 24KB)이 세션에 반영되지 않음.
- **원인**: Claude Code는 작업 디렉토리(cwd)의 CLAUDE.md를 읽는데, PAT Bible 세션이 전부
  `C:\Users\SAMSUNG\Desktop\ai\PAT_Bible`에서 열리고 있었음(세션 109개 vs `C:\projects\PAT_Bible` 0개,
  최근 세션 5개 cwd 전부 Desktop 확인). 그 폴더에 남아 있던 **6월 22일자 14.7KB 옛 지침**이 대신 읽힘.
  2026-06-22 OneDrive 사고 후 소스는 `C:\projects\`로 옮겼으나 **세션 위치만 옛날 그대로**였음.
  강제하는 설정(바로가기·ttapp·MCP config)은 없었고 습관이 원인.
- **부작용**: Playwright MCP는 output-dir 설정이 없어 cwd에 씀 → 스크린샷·콘솔로그 368개(3.9MB)가
  OneDrive 동기화 폴더에 2026-07-15까지 쌓임.
- **조치**:
  1. 바탕화면 `PAT Bible - Claude Code.lnk` 생성 → `C:\projects\PAT_Bible`에서 Claude Code 실행. 사용자 실행 확인.
  2. Desktop 옛 폴더 찌꺼기 58개 휴지통 정리 후 영구 삭제. **소스 파일은 0개**였음(app/·docs/·functions/ 없음).
     삭제 전 확인: Desktop 옛 지침은 정식 지침의 완전한 부분집합(고유 줄 0개).
  3. Desktop 폴더는 **지우지 않고 경고용 CLAUDE.md만 남김**(트립와이어). 실수로 그 폴더에서 세션을 열면
     "여기는 작업 폴더가 아닙니다 — 즉시 중단"을 첫 줄에서 읽게 됨. 폴더를 없애면 습관 재발 시 조용히 다시 생기므로 의도적으로 유지.
- **검증**: 바로가기 실행 시 프롬프트 경로 `C:\projects\PAT_Bible` 확인(사용자). 정식 프로젝트 무손상(1551파일 79.3MB, app/index.html 존재).

### 📄 CLAUDE.md -1 — 지침 내 Desktop 경로 모순 2건 수정
- **증상**: 5~11행은 정식 경로를 `C:\projects\PAT_Bible`로 못박고 Desktop 작업을 금지하는데,
  55행 "실제 작업 폴더"는 `C:\Users\SAMSUNG\Desktop\ai\PAT_Bible`로 남아 있어 지침이 자기모순.
- **수정**:
  - 55행: `C:\projects\PAT_Bible` + "Desktop 경로는 사용 금지" 명시
  - 87행: 종합 문서 경로 `C:\Users\SAMSUNG\Desktop\ai\pat-1단계-기획설계-실행문서.md` → **해당 파일 없음 확인**,
    실재하는 `_assets\docs\pat-1단계-기획설계-실행문서.md`(38KB)로 연결. 다른 문서 참조와 동일하게 상대경로 통일.
- **검증**: 남은 "Desktop" 매치는 전부 금지문 또는 ttapp 자동관리 블록. `git diff` 2줄(2 insertions, 2 deletions).

---

> 📌 **아래 2026-07-03 ~ 07-15 구간은 2026-07-16에 사후 복원한 기록입니다.**
> 당시 세션이 Desktop 옛 경로에서 열려 이 파일의 기록 규칙이 적용되지 않아 약 2주치(v173~v204)가 누락됐습니다.
> 근거: git log 42건 + 메모리. 커밋이 없는 작업은 근거를 명시했습니다.
> 커밋이 확인되지 않는 버전: **v174, v175, v190, v191, v194** (배포 중 결번이거나 기록 없음).

## 2026-07-15

### 🛠 관리자페이지 -2 (v204) — 관리자 페이지 3종 구현 + 딥링크 라우팅 안정화
- `admin-panel.js` 신설(193줄), `router.js` 딥링크 처리 보강
- 커밋 08fa0f2

### 🔐 회원가입 -1 (v204) — 이메일 회원가입 + 관리자 Firebase Auth 분리 + 가족비번 강도규칙
- 신규: `signup.js`, `admin-auth.js`, `router.js`, `functions/admin-api.js`, `scripts/set-admin.mjs`
- 관리자 인증을 Firebase Auth로 분리, `firestore.rules` 권한 재정의, `.env.example` 추가
- 테스트 `signup-admin.test.cjs` 추가. 캐시 v203→v204. 커밋 a6caa2a

## 2026-07-13

### 🔐 백엔드(Functions) -4 — 가족 초대 기반 로그인 v2 백엔드 (Sprint 1)
- 신규: `family-v2.js`, `family-v2-api.js`, `SCHEMA.md`, 테스트 2종(`family-v2.test.cjs`, `family-v2-flow.test.cjs`)
- 백엔드 기반 작업 — 프론트 연동은 별도. 캐시 버전 변경 없음. 커밋 6fee5cb

### 🐛 로그인/입장 -11 (v201 → v203) — 레거시 교회코드 013579 차단 메시지가 안 보이던 문제
- **증상**: 013579로 입장 시 차단은 되는데 안내 메시지가 화면에 안 뜸
- **경과**: v201 토스트 개선 → 표시 안 됨 → 수정1(4fbf790) → 수정2 CSS 클래스 대신 직접 스타일 조작(a1dfce1)
  → 수정3 토스트 직접 표시(57e8956) → v202 토스트 수정(9e8f70d) → **v203에서 `alert()`로 확정**(9ea382b)
- 최종적으로 토스트를 포기하고 alert로 단순화(app-core.js 23줄 축소). 캐시 v200→v203

## 2026-07-12

### ⏪ 로그인/입장 -10 (v200) — 자유입장 가족방 시스템 전면 롤백
- 전날 추가한 자유입장(교회코드 불필요) 기능을 **기존 교회코드 로그인으로 되돌림**
- 제거: `functions/index.js` 163줄, `app-core.js` 276줄 축소, `family.js` 93줄 축소,
  `family-free-entry-security.test.cjs`(202줄), 설계문서 2건(MIGRATION-ANALYSIS-v0, MIGRATION-PHASE2-DECISION)
- 캐시 v199→v200. 커밋 c1462e3

## 2026-07-11

### 🛠 로그인/입장 -9 (v198 ~ v199) — 자유입장 가족방(교회코드 불필요) ※다음날 v200에서 롤백됨
- 백엔드 API 추가(8be30f3) → 프론트 로직 추가, 기존 로그인 흐름 유지(05b7de1) → UI 화면 5개 + 보안 단위테스트(b5a4a24)
- v199: 백그라운드 초기화 로직의 `s-entry` 처리 정확성 개선(7418fb5)
- **결과: 다음날 롤백(v200).** 되돌린 이유는 커밋 메시지에 남아 있지 않음

### 🔧 백엔드(Functions) -3 (v197) — Functions pepper 중앙화 및 일관성 개선
- `functions/index.js` 41줄 수정, 의존성 갱신
- 분석 문서 4건 동시 작성: `013579-login-analysis.md`, `013579-login-root-cause.md`,
  `2026-07-11-BUG-FIX-SUMMARY.md`, `PLAYSTORE-READINESS.md`
- 커밋 282dfea

### 🐛 로그인/입장 -8 (v196) — 레거시 교회코드 013579 완전 차단
- `app-core.js` 차단 로직 추가. 캐시 v195→v196. 커밋 701e7a3

### 🎨 로그인/입장 -7 (v195) — 로그인 오류 메시지에 비밀번호 입력값 확인 안내 추가
- `app-core.js` 문구 보강. 캐시 v194→v195. 커밋 f6423f0

### 🎨 성경읽기표 -12 — 성경 절표시 행높이 정렬
- `sup` → `span` + `inline-block`으로 텍스트 기준선 맞춤 (`reading.js` 1줄). 커밋 07601c5

## 2026-07-09

### 🎨 성경읽기표 -11 — 본문 글자 크기 조절 기능
- `reading.js` 폰트 크기 컨트롤 추가, 테스트 `reading-font-size.test.cjs`(92줄). 커밋 9be77df
- 인수인계 문서 갱신(a9e1c71)

### 📱 캐시 -2 (v193 / 3b3903a) — 폰에 앱 업데이트가 반영되지 않던 근본 문제
- **증상**: 권호택 폰에 '다른 교회 코드로 입장' 버튼이 안 뜸 = 앱 업데이트 자체가 폰에 로드 안 됨
- **원인**: `firebase.json`의 no-cache 규칙이 `"source":"index.html"`이라 `/index.html`만 매칭 →
  TWA가 실제 로드하는 루트 `/`는 기본 `max-age=3600`(1시간 캐싱) 적용 → 폰이 옛 버전 붙잡음.
  SW도 navigate에서 `fetch(request)`로 HTTP 캐시를 그대로 씀
- **수정**: (1) `firebase.json`에 `/` 경로 no-cache/no-store 규칙 추가 (2) `sw.js` navigate fetch `{cache:'no-store'}` 강제
- 자산 캐시는 유지. 기존 캐시된 폰은 1회 캐시 삭제 필요(안드 설정→앱→PAT Bible→저장공간→캐시 삭제)

### 🐛 로그인/입장 -6 (v193) — 로그인 화면 '다른 교회 코드로 입장' 버튼 미표시 수정
- `refreshLoginMode`에서 정상 표시되도록 수정. 커밋 254eb39

### 🔐 로그인/입장 -5 (v192) — 가족방 로그인 갇힘 근본 수정
- **원인**: 권호택 기기에 옛 교회코드 013579(세광 11111 재편 전)가 `pat_church_code`로 잔존 →
  존재하지 않는 `churches/013579` 조회 → 항상 거부. 로그아웃이 `pat_family_id`는 지워도
  `pat_church_code`는 안 지워 갇힘 + 탈출구(다른 교회 선택 버튼)도 07-01에 제거돼 전무
- **수정**: (1) 로그인 화면에 '다른 교회 코드로 입장' 버튼 복원(`resetLoginState` 연결, 저장코드 있을 때만 노출)
  (2) `findFamily`: familyId를 빠른 경로로만 쓰고, 미해결 시 null 대신 전역 비번 검색 폴백(stale familyId 차단 방지)
- 임시함수 patTrace/patToday로 실제 요청 `{churchCode:013579}` 포착해 확정 후 삭제. 커밋 7fc1289

### 🔐 백엔드(Functions) -2 — 가족방 비밀번호 '다릅니다' 근본 해결 (pepper 불일치)
> ⚠️ 커밋 없음(`functions/.env`만 변경). 근거: 메모리 기록 2026-07-09
- **원인**: 저장된 모든 가족 해시(HMAC)는 폴백 pepper `pat-bible-dev-pepper`로 생성됐는데,
  `password.js`가 `functions.config().pat.pepper`(=085f7ad0…)를 읽도록 되어 있어 로그인 시 해시 불일치
- **수정**: `functions/.env`에 `PAT_PASSWORD_PEPPER=pat-bible-dev-pepper` 명시(process.env 최우선) +
  `functions.config()`의 `pat.pepper` 언셋(재발 방지). findFamily 재배포
- 검증: 013579·1111·12345 전부 로그인 성공, 0000 정상 거부

### 🗄 공통/DB -2 — Firestore 테스트 교회 정리 + mojibake 데이터 수정
> ⚠️ 커밋 없음(운영 데이터 작업, 코드 변경 0). 근거: 메모리 기록 2026-07-09
- 테스트 등록 #482913(은혜교회)·#928374(테스트교회) recursiveDelete + adminIds 인덱스 정리, 빈 유령 코드 'SG' 삭제
- **진짜 mojibake**는 11111(세광, 실운영) `config.parishTotals`의 깨진 중복키(hex efbfbd=U+FFFD)였음 →
  정상키(p1_total150/p2_total120/p3_total110/blessing_total30)만 남기고 제거
- 정리 후 무결성 재검증: 세광 가족 3·기록 18 보존, findFamily 정상

### 🎨 성경읽기표 -10 (v189) — '오늘의 성경읽기' 제목 옆 (개역한글) 배지 표시
- 커밋 ff557b2

### 🎨 성경읽기표 -9 (v188) — 오늘의 성경읽기 4트랙에 '개역한글' 라벨 표시
- 커밋 0e84938

### 📖 성경읽기표 -8 (v187) — 개역한글 전체 성경(66권 1189장 31,103절) 투입
- 원본: nehemiaharchives/bbl `krv.zip`(개역한글 1961, 매니페스트 copyright: Public Domain = 저작권 만료 2012)
- `scripts/build-krv-full.cjs`가 zip 추출 txt 파싱 → `krv.json`(7MB) + `krv.version.json` 생성
- 로더 최적화: 가벼운 version 매니페스트 먼저 fetch → 버전 동일 시 7MB 재다운로드 안 함(`bible-loader.js`)
- 교차-책 범위(암8~옵1) 지원: `bible-passage.js`에 66권 장수(_CH_COUNTS) + parseRef segments 구조
- 테스트 10/10 = 365일×4트랙 1460참조 전부 본문 연결(누락 0). Playwright: 31103절 시딩 4.1초. 커밋 d520a82

### 📖 성경읽기표 -7 (v186) — 읽기표 4트랙(시편·구약·신약·잠언) 전부 실제 본문 연동
- 정확도 위해 ibibles.net(개역한글)에서 원문 자동 수집(`scripts/fetch-krv.cjs`)
- 추가 본문: 역대하11~12, 사도행전16:16~40, 잠언1·6, 시편6. krv.json 총 181절
- 테스트 9/9. 커밋 5b74fec. ★공식 대한성서공회 대조 검증 권장

### 🐛 성경읽기표 -6 (v185) — 시편 9편 클릭 시 안내 문구만 뜨던 문제
- 원인: 샘플 krv.json에 시편9가 없어 조회 0건(버그 아님·데이터 부재)
- 시편9 20절 추가, version `krv-sample-2026-07-10`(기존 기기 자동 재시딩). 총 46절. 커밋 bcfcf3c

### 📖 성경읽기표 -5 (v184) — 개역한글 성경 IndexedDB 저장·읽기표 연동 인프라
- 신규: `bible-passage.js`(66권 정경표+ref파서), `bible-db.js`(IndexedDB 래퍼),
  `bible-loader.js`(최초 1회 시딩·버전 변경 시만 재시딩), `reading-progress.js`(완료기록 로컬 우선·중복 방지)
- `reading.js` 본문 표시 → IndexedDB 조회로 교체(오프라인 동작) + 완료 버튼
- 테스트 8/8. 기존 가족방/로그인/순위 미변경. 커밋 3ff9acf

## 2026-07-06

### 🎨 성경읽기표 -4 (v183) — 오늘의성경읽기 메뉴 가로 배치(4열)
- 1열 → `repeat(4,1fr)` 좌우 나란히. 버튼 컴팩트(중앙정렬·상단 accent라인·줄바꿈 허용·화살표 제거)
- 412px 모바일 뷰포트 Playwright 확인. 커밋 370795c

### 🎨 성경읽기표 -3 (v182) — 메뉴 1열 배치 + 구약/신약 트랙 라벨 제거
- 각 버튼 한 줄: 시편/잠언은 '시편 6'·'잠언 6', 구약/신약은 '역대하 11~12'·'사도행전 16:16~40'
- 커밋 9524a78

### 🎨 성경읽기표 -2 (v181) — 오늘의성경읽기 트랙 아이콘 제거
- 시편🎵·구약📜·신약✝️·잠언💡 이모지 삭제(`_READING_TRACKS` emoji ''). 커밋 6e17f02

### 🎨 성경읽기표 -1 (v180) — 버스 아이콘(🚌) 제거
- '🚌 바이블버스 성경읽기표 안내' → '바이블버스 성경읽기표 안내', '🚌 오늘의 성경읽기' → '오늘의 성경읽기'
- 커밋 a8fece5

## 2026-07-05

### ⚡ 로그인/입장 -4 (v179) — 로그인 후 홈 즉시 렌더(체감속도)
- **원인**: 재로그인 시 홈이 `findFamily` + `getConfig`를 순차 await(getConfig 실측 0.5~1.4s가 홈 렌더 블로킹)
- **수정**: `_enterFoundFamily`에서 `enterMemberHome()` 먼저 → `loadChurchConfig()` 백그라운드 fire-and-forget
  (`subscribeConfig`가 자동 재렌더). 네트워크 왕복 2→1
- 신규 테스트 login-render-timing 7/7, 신규 실패 0. 커밋 247a04a

## 2026-07-04

### 🐛 가족방 -6 (v178) — 완료 표시 근본 수정
- **원인**: 권호택=방장인데 서버 이름 mojibake → v177의 이름 정확매칭 실패 → 플레이스홀더 잔존
- **수정**: 완료 = `doneToday` 단일 기준(done 제거·미러링), 자가치유 deviceId 우선 매칭 + 미확정 시 프루닝,
  `s-complete` 복원은 오늘 진짜 기록이 있을 때만
- E2E 10/10. 커밋 dc30915

### 🐛 가족방 -5 (v177) — 완료 자가치유(server-sync 플레이스홀더 정리)
- `family.js` 자가치유 로직 추가, 테스트 `family-selfheal-placeholder.test.cjs`(79줄)
- 캐시 v176→v177. 커밋 aec9c33

### 🐛 가족방 -4 (v176) — 암송 완료가 '오늘 완료'만 반영되도록(자정 초기화 버그 근본 수정)
- `family.js`·`memorize.js` 수정, 설계문서 `design/DESIGN_REFINE_2026-07.md` 작성
- 테스트 2종 추가: `daily-reset-completion.test.cjs`(84줄), `memorize-complete-ssot.test.cjs`(116줄)
- 캐시 v175→v176. 커밋 2e87f25

## 2026-07-03

### 🎨 초기화면 -2 (v173) — 로고 너비를 '세광교회' 텍스트 폭에 맞춤
- 높이 종횡비 유지. 캐시 v172→v173. 커밋 fa8bd7b

---

## 2026-07-02

### 🎨 초기화면 -1 (v172) — 로고 이미지 교체 + '세광교회' 표기
- 로그인 화면 `.brand` SVG 마크 → PAT Bible 로고 이미지(icons/pat-logo.png)로 교체
- 로고 아래 '세광교회' 텍스트(.brand-church) 추가, 세로 배치
- applyAppTitle 호환용 loginAppTitle 숨김 요소 유지(로직 안전)
- 캐시 v171→v172. 라이브 검증: 로고·세광교회 렌더 확인

### 🐛 이번주구절 날짜 -1 (v171) — 오늘 날짜 UTC 오프바이원 버그 수정
- 증상: KST 00:00~09:00 사이 '이번 주 구절'의 오늘/주차가 하루 전으로 표시(오늘 초기화 안 됨)
- 원인: `new Date().toISOString().slice(0,10)`는 UTC 기준 → KST 새벽엔 어제 날짜 반환
- 수정: `patLocalToday()`(로컬 기준) 신설, `renderMemberDateLabels`(verse.js)·admin 기준날짜 기본값(app-core.js)에 적용
- 캐시 v170→v171. 라이브 검증: patLocalToday=로컬 오늘, toISOString=어제 확인(버그 재현·해결)

### 🎨 가족방 -3 (v170) — 삭제 버튼 색상 #4374D9(파랑)
- `.family-member-delete` color: #818181 → `#4374D9` (멤버리스트·칩 삭제 버튼 모두 적용)
- 가족원 이름은 #818181 유지
- 캐시버전 v169→v170. 라이브 검증: 삭제 버튼 `rgb(67,116,217)` = #4374D9

### 🎨 가족방 -2 (v169) — 가족원 이름·삭제 버튼 글자색 #818181
- 멤버리스트 이름(family.js), 등록 칩 이름(family.js), ✕ 삭제 버튼(index.html) 3곳 → `#818181`
- 캐시버전 v168→v169 (sw.js 포함)
- 라이브 검증: 세 곳 모두 `rgb(129,129,129)` = #818181 일치

### 🎨 가족방 -1 — 가족 삭제 버튼 색상을 본문 글씨 색상과 통일
- `.family-member-delete` color: `var(--danger-soft)`(연한 빨강) → `var(--text)`(본문 글씨 색)
- 3연타 확인(arming) 상태는 기존 은은한 danger 톤 유지(실수방지 경고 UI)
- 검증: 라이브(web.app) 렌더 결과 삭제 버튼=본문=`rgb(15,20,25)` 완전 일치

### 🛠 관리자페이지 -1 (v166) — 교구별 현황 '전체 인원'을 등록가정과 자동 동기화(데이터 일관성)
- **증상**: 관리자 '교구별 현황 관리'의 전체 인원이 비어 있어 실천율이 0%로 표시되고,
  '등록가정' 탭의 참여인원(총 9명: 1교구 5, 2교구 4)과 숫자가 불일치.
- **원인**: `applyAggregatedToAdmin()`가 '완료'만 byParish로 채우고, '전체 인원'은
  관리자 수동 입력값만 사용 → 미입력 시 전체=0 → 실천율=완료/0=0%, 등록가정과 불일치.
- **수정**: '전체 인원'을 등록가정과 같은 서버 집계값 `registeredByParish`로 자동 채움.
  관리자 수동 저장값이 있으면 존중, 없으면 등록가정 참여인원으로 자동 동기화.
- **검증(Playwright + 실 Firestore, 세광교회 11111)**:
  registeredByParish={1교구:5, 2교구:4} = 등록가정과 정확히 일치. 자동 채움 후
  1교구 완료5/전체5=100%, 2교구 완료1/전체4=25%로 정상. 수동값(1교구=10) 존중도 확인.
- 파일: `app/index.html`(인라인 admin 스크립트), `?v=165 → ?v=166`.

### 📊 현황페이지 -1 (v165) — 가족방 등실천율 동기화 반응속도 개선
- **증상**: 현황(대시보드)의 가족·교구 등실천율 동기화가 늦게 반영됨.
- **원인**: 클라이언트 폴링은 이미 1초였으나, `computeAggregatedData()`가 독립적인
  Firebase 호출 3개(가족진도·기도완료·교구집계)를 **순차 await** 해서 왕복이 합산돼 느렸다.
  또 기도 완료 반영이 3초 캐시로 지연.
- **수정(모두 클라이언트, 서버 집계 로직 frozen 유지)**:
  - `computeAggregatedData()`: 3개 호출을 **Promise.all 병렬 프리페치** → 지연이 '합계'가 아닌 '최댓값'.
    실측 순차 3607ms → 병렬 2452ms (~32%, 약 1.1초 단축).
  - `fetchPrayedMembersToday()`: 기도 완료 네트워크 캐시 **3초 → 1초**(1초 폴링과 정렬).
- **검증(Playwright + 실 Firestore, 교회 11111)**: 집계 정확(가족 2/5=40%, 교회 67% 3중2),
  대시보드 렌더 정상·예외 없음, 순차/병렬 소요시간 비교로 개선 확인.
- 파일: `app/js/memorize.js`, `app/js/family.js`, `?v=164 → ?v=165`.

### 🙏 기도페이지 -3 (v164) — 리더 기도가 저장돼도 화면에 안 보이던 진짜 버그 수정 (실 Firestore E2E)
- **증상**: "기도 볼 수 있는 창(칩·피드)에 저장이 안 됨" — 저장은 되는데 화면에 안 나타남.
- **진짜 원인**: 리더(방장)의 실제 `memberName`이 **"Guest"**. `currentPrayerMember()`가 'Guest'를 반환 →
  기도가 'Guest' 이름으로 저장됨. 그런데 v157의 **"Guest" 필터**(prayerMemberList·renderPrayerFeed)가
  이를 걸러내서 → 저장돼도 칩/피드에 안 보였다. (권호택 = 리더라 정확히 이 케이스)
- **수정**:
  - `currentPrayerMember()`: memberName이 'Guest'/빈값이면 `leaderName`(실제 이름)으로 대체 → 실제 구성원명으로 저장·표시.
  - getPrayers 병합: 옛 데이터에서 'Guest'로 저장된 기도를 그 가족 `leaderName`으로 리맵 → 기존 기도도 표시.
- **검증(Playwright + 실 Firestore, familyId=mcZYCWhWozBjKAl8x5W9)**:
  실제 권호택 세션(memberName='Guest') 재현 → 저장 시 '권호택'으로 저장 → 칩 🙏 녹색 + 피드 "권호택님의 기도" 표시.
  **로컬캐시 전부 삭제 후 새로고침(다른 기기 시뮬레이션)에도 Firestore에서 복원되어 피드에 정상 표시** 확인.
- 파일: `app/js/prayer.js`, `?v=163 → ?v=164`.

### 🙏 기도페이지 -2 (v163) — 저장이 안 되던 경합 버그 완전 수정 (브라우저 E2E 검증)
- **증상**: 기도를 저장해도 저장이 안 됨(빈 값이 저장됨 / 방금 저장한 기도가 사라짐).
- **원인 1 — 입력 텍스트 소실**: `renderPrayer`가 Firebase `getPrayers` 콜백에서
  입력칸(미포커스)을 서버값으로 덮어써, 아직 저장 안 한 사용자 입력이 지워짐 → 빈 값 저장.
  → 저장된 기도가 있을 때만 채우고, 없으면(빈 값) 입력칸이 비어 있을 때만 비우도록 방어(2곳).
- **원인 2 — 옛 서버값이 방금 저장을 클로버**: 로컬 저장 직후 아직 전파 안 된 옛 서버값(비어있지 않음)이
  getPrayers 콜백에서 로컬을 덮어써 저장이 사라짐.
  → `_prayerJustSaved` 보호 플래그(20초) 도입: 방금 저장한 '내' 기도는 서버 전파 지연 동안 덮어쓰지 않음.
- **검증**: 로컬서버+Playwright로 실제 Firebase 연결 상태에서 E2E — 버튼저장/음성자동저장 모두
  저장 유지 + 콜백 후에도 유지 + 권호택 칩 🙏 녹색 표시 확인.
- 파일: `app/js/prayer.js`, `?v=162 → ?v=163`.

### 🙏 기도페이지 -1 (v162) — 완료 흐름 일원화 + 녹색 확인 버그 수정
- **음성 기도 완료 일원화**: 마이크 off(전사 완료) 시 즉시 자동 저장 →
  별도 하단 '🙏 기도 완료' 버튼을 다시 누를 필요 없음. (저장 후에도 텍스트 남아 수정 가능)
- **녹색 확인 미표시 버그 수정**(권호택 등): 구성원 이름 공백/형식 차이로
  board 조회가 실패해 본인 칩의 🙏(녹색 확인)·✓완료 뱃지가 안 뜨던 문제.
  → `_pnorm`(공백 정규화) + `_prayerFindEntry`(정규화 매칭) 도입, 칩/뱃지/기록 매칭을 모두 정규화 기준으로 통일.
- **Firebase 병합 경합 방어**: 서버가 옛 빈 값을 돌려줄 때 방금 저장한 로컬 기도를
  덮어써 녹색 확인이 사라지던 경합 차단(빈 서버값이 비어있지 않은 로컬 기도를 덮어쓰지 않음).
- 파일: `app/js/prayer.js`, 버전쿼리 `?v=162` 일괄 상향.

---

## 2026-07-01

### 🎯 배포 링크 확정 (v147)
- **성도 배포:** `https://pat-bible-app.web.app` (기본 앱, familyId 없음)
  - 모든 교회 성도에게 공유
  - 교회코드 입력 → 비밀번호 입력 → 입장
- **가족 초대:** `?invite={churchCode, familyId, ...}` (가족원만)
  - 대표가 "👨‍👩‍👧 가족 초대" 버튼으로 생성
  - familyId 자동 설정
- **공개 링크:** `?join={churchCode, isPublic:true}` (다른 성도)
  - 대표가 "🌐 공개 링크" 버튼으로 생성
  - 검색해서 참여

### 📲 로그인 화면 최종 정리 (v145-147)
- 입력창 높이 축소: `height:44px`, `padding:8px 12px`
- "교회 코드를 입력하세요" 표시 제거 (`display:none`)
- "🔄 다른 교회 선택" 버튼 삭제
- 비밀번호 입력 시 placeholder: "비밀번호" (교회코드 텍스트 제거)

### 🔧 개발자 메뉴 삭제 (v144)
- "🔧 개발자 (11111)" 버튼 제거
- `devEnterChurch()` 함수 제거

### 👨‍👩‍👧 가족 등록 버튼 일원화 (v141)
- "👨‍👩‍👧 가족 등록" 버튼 삭제 (가족방에서)
- "👑 대표 등록" 만 남김 (대표가 통일관리)
- 초대링크 + 재설치 시에도 비밀번호만 입력

### 🔗 두 종류 링크 분리 (v143)
- 👨‍👩‍👧 가족 초대: `?invite=...` (familyId 포함)
- 🌐 공개 링크: `?join=...` (familyId 없음)

### 📲 앱 재설치 후 로그인 안내 개선 (v140)
- **상황:** 사용자가 앱을 삭제 후 재설치하면 localStorage 초기화 → "새 기기" 경로로 진입 → "가족 등록" 페이지에서 이름 입력 필요. 그런데 왜 "가족 등록" 화면이 나오는지 불명확해서 사용자 혼동 발생.
- **개선:**
  1. **로그인 페이지 안내:** "📲 폰을 바꾸거나 앱을 다시 설치했나요?" 섹션을 더 명확하게(체크마크 + 색상 개선). "새 기기" 메시지 언급.
  2. **가족 등록 페이지 안내:** "ℹ️ 새 기기에서 처음 입장합니다" 정보 박스 추가. 앱 재설치·기기 변경·새로운 구성원 구분 설명.
  3. **콘솔 로그:** `[LOGIN-STEP2-NEW-DEVICE]` 추가해서 개발자가 문제 추적 용이.
  4. **토스트 메시지:** "가족방을 찾았어요! 새 기기에서 처음 입장하므로 이름을 입력해 합류하세요" → 더 명확한 가이드.
- **동작(정상):** 교회코드 + 가족비번 입력 → 가족방 찾음 → (기존 기기: 홈 진입) (새 기기: 이름 입력 필요) → 이름 입력 후 제출 → 홈 진입 + 모든 데이터 복원.
- **파일:** `app/index.html`, `app/js/app-core.js`.

### 🔐 로그인 오류 근본 원인 분석 완료 (2026-07-01)
- **보고 증상:** "폰에서만 비밀번호가 다르다고 나옴" (웹에선 정상)
- **추적 과정:**
  1. Service Worker 캐시 문제 → v=132→v=139로 업데이트
  2. churchCode 저장 누락 → localStorage에 저장 추가
  3. 교회코드=비밀번호 검사 제거 → 서버가 어차피 검증
  4. **최종 발견:** 폰에 로컬 데이터 없어서 "새 기기" 경로 진입 정상 → 이름 입력 필요
- **결론:** 앱 로직/서버 오류 없음. 재설치 후 새 기기이므로 이름 입력이 필수 동작. 다만 사용자가 이것을 인지하지 못해서 혼동 발생 → 안내 개선으로 해결.
- **테스트:** API 직접 호출로 `findFamily` 정상 동작 확인. 서버에서 가족방 정상 반환.

---

## 2026-07-01 (이전 항목)

### 📲 카카오 인앱브라우저 설치 개선 — 버튼 1탭으로 기본 브라우저 열기 (v133)
- **문제:** 카카오톡으로 링크를 받으면 인앱 브라우저라 설치 불가. 기존엔 "오른쪽 위 메뉴 → 다른 브라우저로 열기" 안내 alert만 떠서 사용자가 직접 찾아야 했음.
- **개선:** 인앱에서 "🌐 다른 브라우저로 열기" 버튼 → **탭 한 번에 기본 브라우저(Chrome/Safari)로 즉시 실행**.
  - 카카오톡: `kakaotalk://web/openExternal?url=...` 전용 스킴.
  - 라인: `?openExternalBrowser=1`. 안드로이드 기타 인앱: Chrome `intent://`(미설치 시 폴백). iOS 기타 인앱: 강제 불가 → 안내.
- 인앱 감지 시 버튼 라벨/안내 문구 자동 전환. `externalOpenTarget` 순수함수 분리(라이브 검증 완료).
- **파일:** `app/js/pwa-install.js`, `app/index.html`.

### 📱 홈화면 설치(PWA) 개선 — 무캐시 SW 등록 + 설치 유도 UI (v132)
- **원인:** head 스크립트가 SW를 매번 unregister → 활성 SW 없음 → Android Chrome 설치조건(“fetch 핸들러 가진 SW”) 미충족 → `beforeinstallprompt` 미발생 → 설치 버튼/팝업이 동작 안 함(수동 설치만 가능해 어려웠음).
- **완전 자동 설치 불가(정책):** 어떤 브라우저도 사용자 제스처 없는 자동 설치 불허. Android는 `prompt()`가 클릭 안에서만 호출 가능(버튼 1탭 필수), iOS Safari는 `beforeinstallprompt` 자체가 없어 ‘공유→홈화면 추가’ 안내만 가능. (코드 주석에 명시)
- **수정:**
  - `sw.js`를 **무캐시 패스스루 SW**로 재작성(fetch 핸들러 보유 → 설치조건 충족). 아무것도 캐시 안 함 → 옛 파일 데드락 없음, `?v=` 버전관리 유지.
  - head 스크립트: unregister 로직 제거 → `sw.js` 등록으로 변경.
  - 로그인 화면에 설치 유도 카드(제목/설명/버튼/“나중에 하기”) + iOS 안내 모달 추가.
  - `pwa-install.js`(신규): Android=네이티브 설치 팝업, iOS=안내 모달, 이미 설치/PC=숨김, ‘나중에 하기’=7일 유예.
- **데이터 안전:** SW/설치 로직은 localStorage/Firestore(로그인·가족·미션 기록) 미접근. 캐시 삭제는 HTTP 캐시만(로컬 데이터 무관).
- **파일:** `app/sw.js`, `app/index.html`, `app/js/app-core.js`, `app/js/pwa-install.js`(신규).

### 🔐 가족방 로그인 구조 수정 — 교회코드 인증 제거 (v131)
- **증상:** 가정 비밀번호로 로그인 안 됨 / 교회 코드로 가족방 입장됨.
- **원인:** `enterChurch()`가 하나의 입력창에서 교회코드·가족비번을 겸용. 분기3에서 교회코드를 `getConfig`로 조회되면 그대로 `enterMemberHome()`(가족방 진입). 또 가족비번 검증은 `DB.church.code`가 있어야만 동작 → 교회 미선택 신규기기는 가족비번이 안 먹고 교회코드만 통과.
- **수정:** 2단계 구조로 분리. (1) 교회 미선택 → 입력값=교회코드로 **교회 선택만**(입장 안 함) (2) 교회 선택됨 → 입력값=가족비번으로만 인증. 교회코드를 비번칸에 넣으면 거부. 판정은 순수함수 `loginDecision`(신규 `login-auth.js`)로 분리.
- **다른 가정/구역 차단:** `findFamilyByPassword`가 `pat_family_id` 스코프라 내 가족 외 비번은 실패. 구역 로그인(`joinAnotherRoom`)은 원래 비번(`findFamilyByPasswordGlobal`)으로만 인증 — 교회코드 무관(이미 정상).
- **UI:** 로그인 화면이 교회 선택 후 "가족 비밀번호" 모드로 전환 + 교회 배지 + 대표/가족 등록 버튼 노출. `resetChurchSelection()`으로 교회만 재선택(데이터 보존).
- **데이터:** 인증 로직만 변경 — 미션 기록/진행률/오늘·일·주·월 데이터 일절 미변경.
- **테스트:** `tests/family-login-auth.test.cjs` 신규(요구 필수 케이스 전부 통과).
- **파일:** `app/js/app-core.js`, `app/js/login-auth.js`(신규), `app/index.html`.

### 가족방 등록 화면 중복 탭바 제거 (v126)
- 가족방 등록(=대표 등록/구역방 입력) 화면의 위쪽 중복 메뉴바(`메뉴 바 1`) 삭제. 하단 고정 탭바만 유지.

### 🐞 예배 동기화 안 됨 수정 (v128)
- **증상:** 관리자가 예배 저장 → 성도 예배 탭에 "등록된 예배 안내가 없습니다"만 표시.
- **원인:** `worship.js`가 `window.DB`를 참조. 그러나 `DB`는 `const DB`(전역 렉시컬)라 **window 속성이 아님** → `window.DB`는 `undefined` → 항상 빈 상태로 렌더. (서버 저장·getConfig·applyCloudConfig는 모두 정상이었음 — Playwright로 서버값 확인됨)
- **수정:** `renderWorship`/`loadWorshipToAdmin`에서 `window.DB` → `DB` 직접 참조(`typeof DB` 가드)로 교체. 배포본에서 제목·내용·교회명 정상 표시 확인.

### ✝️ 예배 메뉴 추가 (v127)
- **목적:** 관리자가 예배 안내(제목·내용)를 입력하면 전 성도의 하단 **예배** 탭에 실시간 표시.
- **하단 탭바:** 읽기표 ↔ 현황 사이에 `예배(✝️, s-worship)` 탭 추가(7탭).
- **성도 화면:** `s-worship` — `renderWorship()`이 `DB.worship`을 카드로 표시(줄바꿈 유지), 없으면 안내 문구.
- **관리자:** 탭 메뉴에 `✝️ 예배` 추가(`adminTabWorship`). 제목/내용 입력 + 등록/지우기. `switchAdminTab` 인덱스 재매핑(worship=[2], award=[3], families=[4], password=[5]).
- **저장소:** 교회 `config.worship = {title, content}`(교회 공통). `firebase-db.saveWorship()` → `saveConfig` 엔드포인트에 worship만 부분 저장(관리자 토큰).
- **FREEZE 추가수정(명시적 요청):** 백엔드 `saveConfig`(worship 부분저장 필드 추가) / `getConfig`(worship 반환) / 프론트 `applyCloudConfig`(worship 미러링) — **기존 로직 불변, 필드만 추가**. `subscribeConfig` 해시에 worship 포함(실시간 반영).
- **신규 파일:** `app/js/worship.js`.

## 2026-06-30

### 소그룹 4단계 — 가정별/구역별 시상 분리 (v125)
- **목적:** 그룹 단위 시상을 종류별로 분리(가정끼리 / 구역끼리), 교구 현황은 가정만 집계(구역이 가족 통계 오염 방지).
- **getDashboard(교구 현황, FREEZE 수정):** 가정 종류만 집계 — families/records/prayers/totalFamilies를 `groupType==='가정'`(누락=가정)로 필터. 구역은 제외. 하위호환(기존=가정) 100%.
- **getAwardRanking(FREEZE 수정):** `groupType` 쿼리 파라미터 추가(미지정=가정). 해당 종류 families만 순위. `rankFamilies`는 familyId 매칭이라 3단계의 방별 기록이 각 그룹 순위에 그대로 반영.
- **firebase-db:** `getAwardRanking(churchCode, groupType)` 래퍼 확장.
- **관리자 UI:** 시상 관리 탭 순위 카드에 **🏠 가정 / 🧩 구역 토글** 추가(`setAwardGroupType`). 가정/구역 순위를 분리 조회.
- **테스트:** `award-grouptype.test.cjs` — rankFamilies가 전달된 종류별 families만, familyId로 분리 집계(1회 완료가 양쪽 그룹에 반영) 2/2 통과. 전체 신규 회귀 0.
- 파일: `functions/index.js`, `app/firebase-db.js`, `app/js/admin.js`, `app/index.html`(?v=125), `app/sw.js`(v125), `tests/award-grouptype.test.cjs`(신규).
- **소그룹 1~4단계 + 보안A 완료.** (가정/구역 분류 → 다중 소속 → 1회 완료 전체 반영 → 종류별 시상 분리 + 비번 중복 방지)

### 보안 A — 비밀번호 중복 방지(방마다 고유 비번 강제) (v124)
- **문제:** 한 비번을 두 방(예: 가족방·구역방)에 쓰면, 비번 입장 시 서버가 한 방만(limit 1) 임의 반환 → 구역원이 엉뚱한 방(가족방)에 입장해 **사생활(진도/기도) 노출** 위험.
- **백엔드(`saveFamily`):** 같은 교회 내 다른 방이 동일 비번 해시를 쓰면 **409 DUPLICATE_PASSWORD**로 거부. 재등록 대상(dup)/본인 방은 제외해 정상 갱신은 허용.
- **클라이언트(`saveFamilyProfileAsLeader`):** 저장 전 `findFamilyByPasswordGlobal`로 사전 검사 → 다른 방과 충돌 시 안내 후 중단(로컬 저장도 안 함). 네트워크 오류 시 서버 409가 2차 차단.
- **안전 입장 안내:** 초대 링크는 방 고유 ID를 포함해 같은 비번이어도 안전(특정 방 지정). 비번 입장만 충돌 위험 → 중복 방지로 차단.
- 파일: `functions/index.js`, `app/js/family.js`, `app/index.html`(?v=124), `app/sw.js`(v124).

### 소그룹 3단계 — 1회 완료 → 속한 모든 그룹에 반영 (v123)
- **목적:** 그룹 단위 시상에 맞게, 한 사람이 암송을 1회 완료하면 그 사람이 속한 **모든 방(가정+구역…)**에 완료로 잡히게.
- **구현:** `completeMemorize`가 `pat_rooms`의 각 방마다 `PAT_DB.saveRecordCtx`로 1건씩 저장(방별 familyId/교구/구역/이름/종류 태깅). 방 목록 없으면 기존 활성 방 폴백(`saveRecord`).
- **firebase-db:** `saveRecordCtx(churchCode, record, ctx)` 추가(명시 컨텍스트 저장, add-only). 기존 `saveRecord`는 활성 방용으로 위임. 둘 다 `groupType` 포함.
- **백엔드:** `saveRecord`가 `groupType` 필드도 저장(시상 분리용, 4단계 기반). add-only 유지 — 기존 기록 미변경.
- **집계 반영:** 각 방의 기존 집계(getFamilyProgress/getDashboard/getAwardRanking, familyId 매칭)가 그대로 그 방에 카운트 → 추가 집계 변경 불필요.
- **테스트:** `mission-multiroom.test.cjs` 신규 — 두 방 소속 시 saveRecordCtx 2회(가정+구역, 종류/이름 정확) / 방 목록 없으면 폴백. 2/2 통과. 전체 신규 회귀 0.
- 파일: `app/js/memorize.js`, `app/firebase-db.js`, `functions/index.js`, `app/index.html`(?v=123), `app/sw.js`(v123), `tests/mission-multiroom.test.cjs`(신규).

### 소그룹 2단계 — 다중 소속(여러 방) + 방 전환/추가 (v122)
- **목적:** 한 사람이 가정 방장이면서 구역장도 될 수 있도록 "여러 방 소속 + 전환" 지원. (기존 "한 기기=한 방" 제약 해소)
- **설계 원칙:** "활성 방"(`pat_family_id`/`pat_family_profile`)은 그대로 → 폴링/기록/삭제 등 기존 로직 전부 무변경. 그 위에 **내 방 목록(`pat_rooms`)** + 전환/추가 레이어만 추가(`app/js/rooms.js` 신규).
- **방 목록:** `upsertRoom`(familyId 기준 누적, 삭제 없음), `seedRoomsIfNeeded`(기존 단일방 사용자 자동 시드), `roomToProfile`. 대표 등록·멤버 참여·초대 모든 경로에서 목록에 등록.
- **방 전환:** 가족방 상단 `roomSwitcher`에 칩으로 표시(2개 이상일 때), 탭하면 활성 방 교체. 미션/기록은 활성 방 기준 저장(= 3단계에서 '모든 소속 반영'으로 확장 예정).
- **다른 방 추가:** `s-add-room`(새로 만들기/기존 참여), `s-join-room`(비번 참여). `findFamilyByPasswordGlobal`로 현재 방과 무관하게 전역 검색 → 참여(기존 "다른 방 입장 불가" 제약 해결).
- **백엔드:** `saveFamily` 중복판정에 `groupType` 추가 → 같은 대표+교구라도 가정/구역은 별도 방으로 생성(병합 안 됨). (saveFamily는 FREEZE 목록 아님)
- **보호:** 로그아웃 시 `pat_rooms` 정리, `?reset`에는 `pat_rooms` 보존. `_creatingNewRoom` 플래그는 홈 복귀/대표등록 진입 시 리셋.
- **테스트:** `rooms.test.cjs` 신규(upsert/시드/전환/매핑) 4/4 통과. 전체 신규 회귀 0.
- 파일: `app/js/rooms.js`(신규), `app/js/family.js`, `app/js/app-core.js`, `app/firebase-db.js`, `functions/index.js`, `app/index.html`(?v=122), `app/sw.js`(v122), `tests/rooms.test.cjs`(신규).

### 방 종류 토글 버튼 (가정/구역 나란히) v121
- 방 종류 선택을 드롭다운 → 세그먼트 토글 버튼(🏠 가정(가족방) / 🧩 구역(구역방))으로 변경. 숨김 input으로 값 보관해 기존 로직 무변경.

### 소그룹 1단계 — 방 종류(가정/구역) 분류 필드 추가 (v120)
- **목적:** 가족방 구조 변경 없이 구역모임 등 소그룹을 같은 시스템에서 운영하기 위한 **1단계(분류 필드)**. (다중 소속·완료 반영·시상 분리는 2~4단계 예정)
- **추가:** `groupType: '가정' | '구역'`. 누락 시 '가정'으로 간주 → **기존 데이터 100% 호환**.
- **등록 폼:** 방 종류 선택(가정/구역) 드롭다운 추가. 선택에 따라 라벨/플레이스홀더 전환(`onGroupTypeChange`). 데이터·레이아웃 구조 동일.
- **저장/동기화:** `saveFamilyProfileAsLeader`가 groupType 저장. firebase-db `saveFamily`/`getFamilyInfo` 및 백엔드 `getFamilyProgress`/`getFamiliesList` 응답에 groupType 포함. 폴링이 groupType 동기화(값 있을 때만). 멤버 참여/초대/자동복구 경로에도 groupType 전달.
- **백엔드:** `FAMILY_ALLOWED_FIELDS`에 `groupType` 추가(화이트리스트). 기존 FREEZE 집계함수(getDashboard/aggregate)는 미변경 — 시상 분리는 4단계에서.
- **표시:** 방 종류에 따라 제목 아이콘/문구(가족방↔구역방), 대표↔구역장 라벨 전환.
- **헬퍼:** `groupTypeOf(profile)`(기본 가정), `groupLabels(type)`.
- **테스트:** `family-delete-preserves-records.test.cjs`에 groupType 케이스 추가(기본 가정·구역 등록 저장) → 6/6 통과.
- 파일: `app/js/family.js`, `app/index.html`(?v=120), `app/firebase-db.js`, `functions/index.js`, `functions/password.js`, `app/sw.js`(v120), `tests/`.

### 가족방 구성원 삭제 -2 (실수 방지: 3번 연속 터치 안전장치 v119)
- **증상/요구:** 구성원 삭제 버튼이 너무 쉽게 터치돼 실수 삭제 위험 → confirm 한 번으론 부족.
- **수정:** 삭제를 **3번 연속 터치**로만 실행(`armDeleteFamilyMember`). 1·2번째는 라벨이 `한 번 더 (2)`/`한 번 더 (1)`로 바뀌고 빨강 강조, 3번째에 실제 삭제. 3초 내 다음 터치 없으면 자동 리셋. 첫 터치 시 안내 토스트.
- **폴링 견고화:** 가족방은 1초마다 재렌더되므로 카운트를 **전역 상태(`_famDelArm`)**로 보관 → 재렌더에도 진행상태 유지. 다른 구성원 터치 시 카운트 리셋(엉뚱한 사람 삭제 방지).
- 기존 `confirm()` 제거(3연타가 게이트 역할). `deleteFamilyMember`(실삭제 로직)는 변경 없음 — 미션 기록 미접촉 원칙 유지.
- **테스트:** `family-delete-preserves-records.test.cjs`에 2케이스 추가(3연타 게이트·대상전환 리셋) → 총 5/5 통과.
- 파일: `app/js/family.js`, `app/index.html`(?v=119), `app/sw.js`(v119), `tests/`.

### 수행 기록(history) -1 (일·주·월 조회 + 관리자 시상 근거 + 오늘상태/기록 분리 v118)
- **목표:** 오늘 수행은 날짜가 바뀌면 새로 시작되지만, 수행한 모든 결과는 일/주/월 데이터로 영구 보존·조회. 관리자도 조회해 연말 시상 근거로 사용.
- **데이터 구조:** 오늘 상태(`pat_daily_tasks_*`/`pat_read_done_*`/`pat_prayer_*`, 날짜키·화면은 todayKey 필터) ↔ 영구 기록(서버 `records` add-only + 로컬 미러 `pat_hist_<날짜>`)로 **분리**. 서버 records가 권위 저장소(날짜=createdAt, familyId/memberName/verseRef/읽기(voice)/쓰기(type)/완료시간).
- **백엔드 신규 `getMissionHistory`(읽기 전용):** 가족 단위 GET(churchCode+familyId+from~to, 암송∪기도), 교회 전체 POST(관리자 인증, 시상 근거). 어떤 데이터도 생성/수정/삭제하지 않음. 기존 FREEZE 함수 미변경.
- **프론트:** `app/js/history.js` 신규 — 로컬 미러 누적 저장(`recordMissionHistory`, 날짜별·갱신·삭제없음) + 일/주/월 조회 화면(가족 완료율·개인별 완료여부·전체 완료율, 날짜/주/월 선택). 가족방에 `📊 수행 기록 보기` 버튼, 관리자 시상관리 탭에 `📅 기간별 수행 기록 조회` 추가.
- **기록 저장:** `completeMemorize`에서 history 미러 누적(오늘 상태와 분리). `firebase-db.js`에 `getMissionHistory`/`getMissionHistoryAdmin` 래퍼.
- **보호장치:** `?reset` keepPrefixes에 `pat_hist` 추가 → reset에도 기록 보존. (v117에서 날짜변경 시 records 삭제 이미 차단)
- **테스트:** `tests/mission-history.test.cjs` — 날짜별 누적/갱신/독립, 기간조회, 날짜변경 후 기록·records 보존, 어제 기록 조회 (4/4 통과). 전체 스위트 신규 회귀 0건(FAIL 수 동일).
- 파일: `functions/index.js`, `app/firebase-db.js`, `app/js/history.js`(신규), `app/js/memorize.js`, `app/js/app-core.js`, `app/index.html`(?v=118), `app/sw.js`(v118), `tests/mission-history.test.cjs`(신규).

### 미션 데이터 -2 (날짜변경/배포 시 과거 수행 기록 삭제 — 재발 근본 차단 v117)
- **증상:** v116 배포 후 "기존 가족별 미션 수행 데이터가 삭제"되는 초기화가 재발.
- **근본 원인:** `voice.js`의 `resetMemorizeProgress()`가 날짜 변경 시 `pat_records`에서 **오늘 것만 남기고 과거 기록을 전부 삭제**. `checkAndResetByDate()`는 페이지 로드/화면 전환마다 실행됨(app-core.js:158·690). **v116 버전업 → 자기파괴 SW가 모든 기기를 강제 새로고침 → 로드 시 날짜 오판이 있는 기기는 과거 기록이 통째로 삭제** = 데이터 초기화 재발. (구성원 삭제 버튼 자체는 무관 — 배포가 방아쇠)
- **데이터 위치 분석:** 미션 수행 기록 = 로컬 `pat_records` + 서버 `churches/{code}/records`. 서버 records는 `.add()`(추가전용)이며 **삭제하는 함수는 `deleteFamily`(관리자 전용)뿐**. 구성원 삭제(`removeFamilyMember`)는 명단 배열+입장기록만 정리하고 records는 미접촉.
- **수정1(핵심):** `resetMemorizeProgress`가 **`pat_records`를 일절 삭제/정리하지 않도록** 변경. '진행 중' 세션(음성/타이핑 단계)만 초기화. 오늘 달성률은 어차피 날짜 필터로 계산하므로 전 기록 보존해도 화면 정상.
- **수정2(보호장치):** `resetData()`(체험용 전체삭제 버튼)에 **확인창** 추가 — 실수 삭제 차단.
- **수정3(분리원칙):** `deleteFamilyMember`에 "가족 명단만 변경, 미션 기록 절대 미접촉" 주석 명시.
- **회귀 테스트 추가:** `tests/family-delete-preserves-records.test.cjs` — ①날짜변경 시 과거기록 보존 ②구성원 삭제 시 기록 보존 ③대표 삭제 시 기록 보존+대표 승계. (3/3 통과)
- 파일: `app/js/voice.js`, `app/js/memorize.js`, `app/js/family.js`, `app/index.html`(?v=117), `app/sw.js`(v117), `tests/`.

### 가족방 구성원 삭제 -1 (구성원 페이지 삭제 버튼 + 대표 삭제 허용 v116)
- 가족방 구성원 목록(`memberList`, `renderFamilyMemberList`)의 **각 구성원마다 `✕ 삭제` 버튼** 노출 — 잘못 입력 시 즉시 삭제 가능. (가족방 등록된 경우에만 표시)
- **대표(leaderName)도 삭제 가능**하도록 변경: 삭제 시 남은 첫 구성원으로 대표 자동 승계(없으면 비움) → fallback/서버 폴링으로 옛 대표 부활 방지.
- `deleteFamilyMember` 개선: 확인창 추가, `familyMemberNames` 전체 명단 기준 재구성, `pat_leader_family_profile` 백업 동기화, `saveFamily`로 서버 leaderName/명단 반영, `renderFamily()`로 전체 갱신.
- 인원 제한 없음(기존 유지).
- 참고: 기존 `renderRegisteredFamilyRoom` 삭제 칩은 HTML에 없는 요소(`registeredFamilyMembers`)를 대상으로 해 실제 미노출 상태였음 → 실제 보이는 `memberList`로 이전.
- 파일: `app/js/family.js`, `app/index.html`(?v=116), `app/sw.js`(v116).

### 바이블버스 안내 -1 (안내문 추가 + 본문 안내 문구 수정 v114)
- 성경읽기표(바이블버스) 화면 상단에 **바이블버스 성경읽기표 안내** 카드 추가: "구약·신약 1년 1독, 시편 2독, 잠언 12독…온 가족이 말씀과 기도로 동행…다음 세대에 신앙 유산" 소개문.
- 기존 안내 문구 `오늘의 바이블버스 본문을 눌러 읽으세요` → **`오늘의 바이블버스 본문은 아래 버튼을 눌러 주세요`** 로 변경.
- 파일: `app/index.html`, `app/sw.js`(v114).

### 집계 통일 -1 (가족 실천 0% + 교구 기준 불일치 + 구성원 축소 — 근본 수정 v112)
- **증상:** ①가족 실천 0%(완료자 있는데) ②교구 1교구 2/2인데 가족 실천 0%(기준 불일치) ③입장 직후 분모가 1로 줄고 구성원 사라짐.
- **데이터 진단(서버 실측):** 현재 구절 암송 완료 0명, 오늘 기도 2명(권호택·엄마현 보존됨), 교구 참가 각 2명. 즉 **기도는 서버에 멀쩡히 있고**, "완료=암송 OR 기도"면 가족 실천 2/2가 정답.
- **원인A — 구성원 축소(family.js `refreshFamilyProfileByPassword`):** `findFamilyByPassword`가 반환하는 `found.members`는 **대표 선언 명단만**(입장 구성원 제외). 이를 그대로 `pat_family_profile.members`에 덮어써 입장 구성원(엄마현)이 사라지고 분모가 1로 축소(0/1). → **로컬 보유 ∪ 서버 선언 합집합**으로 보존하도록 수정.
- **원인B — 교구/가족 기준 불일치(functions `getDashboard`):** 가족 실천은 "암송 OR 기도"인데 교구 현황은 **암송 records만** 집계 → 기도만 한 구성원이 가족엔 잡히고 교구엔 0. → `getDashboard`가 오늘(KST) 기도 구성원을 완료 레코드로 합산(`암송 ∪ 기도`)해 `countCompletedMembersByParish`에 전달. 이제 개인·가족·교구 모두 **"암송 OR 기도"** 동일 기준.
- **검증(Playwright + API):** ①입장 직후 분모 2 유지, 구성원 [권호택,엄마현] 보존 ②가족 실천 0/2→2/2(기도 반영) ③getDashboard completedMembers=2, 1교구 byParish=2(기도 반영) — 모두 통과.
- 파일: `app/js/family.js`(A), `functions/index.js`(B, getDashboard), `app/index.html`·`app/sw.js`(?v=112)

### 미션 데이터 -1 (오전 9시 미션 기록 초기화 치명 버그 수정 — UTC 날짜)
- **증상:** A폰에서 미션(암송) 모두 수행했는데 데이터가 초기화됨.
- **진짜 원인:** `voice.js`의 `getTodayDateString()`이 **UTC 기준**(`toISOString`)으로 남아있었음(v102에서 기도/암송 다른 날짜 로직은 로컬로 고쳤으나 이 함수만 누락). → **한국시간 자정~오전 9시**에 미션 수행 시 `pat_memorize_session_date`에 UTC 날짜(어제)가 저장됨 → **UTC가 자정을 넘는 KST 오전 9시**에 `checkAndResetByDate`가 "날짜 변경"으로 오판 → `resetMemorizeProgress`가 **`pat_records`(미션 기록) 통째 삭제**.
- **수정1:** `getTodayDateString()`을 **로컬(KST) 날짜**로 변경 → `todayKey`/`_localDateStr` 등과 통일. KST 자정에만 초기화(정상). 오전 9시 오판 제거.
- **수정2(이중 안전장치):** `resetMemorizeProgress`가 날짜 변경 시 `pat_records`를 통째 지우지 않고 **`completedAt`을 로컬 날짜로 환산해 "오늘 완료한 기록은 보존"**, 어제 이전만 정리. → 향후 날짜 오판이 또 생겨도 오늘 미션 데이터는 안 날아감.
- **자기파괴 SW 무관:** self-destruct SW/kill-switch는 `caches`만 삭제하고 `localStorage`는 건드리지 않음 → 데이터 초기화의 직접 원인 아님(다만 강제 새로고침이 `checkAndResetByDate` 트리거 빈도를 높였을 수 있음. 근본은 UTC 버그).
- **복구 안내:** 서버 records 에도 해당 완료 기록이 없어(saveRecord 미도달 또는 함께 유실) 자동 복구 불가 → 다시 미션 수행 필요. 단 이제 오전 9시 초기화는 없음.
- 파일: `app/js/voice.js`, `app/index.html`(?v=111)

### 캐시 -1 (자기파괴 SW로 옛 버전 데드락 완전 해소 — v110)
- **증상:** 캐시버스팅(?v=109)·kill-switch 강화 후에도 B폰이 여전히 옛 버전(달성률 3/3, maxScore=3). A폰은 새 버전(0/2).
- **확인:** 코드엔 `maxScore=2`만 존재(grep 재확인). 3/3은 100% 옛 캐시. **코드 버그 아님.**
- **남은 데드락:** B폰의 **옛 서비스워커가 옛 index.html을 cache-first로 계속 제공** → 새 index.html(?v=110, kill-switch)이 로드되지 못해 캐시버스팅이 무력화.
- **근본 수정:** `sw.js`를 **자기파괴(self-destructing) SW**로 교체. 옛 SW가 이 새 sw.js를 자동 업데이트로 받는 순간 activate에서 ① 모든 캐시 삭제 ② 제어 중인 모든 탭 강제 새로고침(`client.navigate`) ③ 자기 자신 `unregister`. → 새로고침된 탭은 SW 없이 서버에서 최신 index.html + 최신 JS(?v=110)를 받아 데드락이 스스로 풀림. fetch는 network-only(캐시 미사용).
- **이중 안전:** index.html kill-switch(옛 SW 감지 시 1회 reload) + 자기파괴 SW 둘 다 동작. 정상 기기(SW 없음)는 무영향.
- **버전 관리:** 앱은 더 이상 SW 캐싱 미사용. JS 버전은 index.html `?v=`로만 관리. `bump-version.sh`가 index.html `?v`와 sw.js 버전 주석을 함께 갱신.
- 파일: `app/sw.js`(자기파괴 SW로 전면 교체), `app/index.html`(?v=110), `bump-version.sh`

### 가족방 -1 (기기마다 가족방 데이터 다르게 보임 — 옛 SW 캐시 데드락 자동 해소)
- **증상:** 같은 가족방인데 폰A는 정상(예운네 말씀방, 권호택·엄마현 ✓완료), 폰B는 가족 데이터가 없고 "내 이름/가족 비밀번호/가족 등록하기" 폼 + 탭바가 2개 중복.
- **진단(결정적):** 폰B 화면의 placeholder가 `예: 이영희`인데 **현재 코드는 `예: 김민수`**. s-family 미입장 화면도 현재는 "등록된 가족방이 없습니다" 메시지뿐인데 폰B엔 인라인 등록폼. → **폰B가 옛 캐시 버전(옛 index.html+JS)을 로드 중**. 탭바 중복·데이터 차이 모두 옛 버전 특징.
- **현재 배포 검증(Playwright):** 탭바 1개, placeholder "예: 김민수", s-family에 인라인 폼 없음 — **현재 버전은 정상**. 즉 코드 버그가 아니라 폰B의 옛 캐시 문제.
- **근본 원인(데드락):** index.html에 SW 해제 kill-switch가 있지만, **옛 SW가 옛 index.html을 계속 제공하면** kill-switch가 든 새 index.html이 로드되지 않아 `?v=109` 캐시버스팅이 무력화됨.
- **수정(자동 해소):** kill-switch 강화 — ① caches 삭제를 SW 유무와 무관하게 즉시 실행 ② **옛 SW가 있었으면 `sessionStorage` 플래그로 세션당 1회 강제 `location.reload()`** → 다음 로드는 SW 없이 최신 index.html + 최신 JS(?v=109)를 받아 옛 캐시 데드락이 스스로 풀림. (무한 새로고침은 플래그로 방지)
- **안내:** 가족방 데이터는 서버(Firestore)에 정상 보존. 폰B가 새 버전으로 갱신된 뒤 이름+가족비번(또는 초대링크)으로 입장하면 폰A와 동일하게 보임. 각 기기는 localStorage 기반이라 기기마다 1회 입장 필요(정상 동작).
- 파일: `app/index.html`(kill-switch 강화)

### 홈 화면 -2 (기기마다 달성률 다름 2/2 vs 3/3 — 옛 JS 캐시 근본 해결)
- **증상:** 같은 날, 한 사람 폰은 오늘의 달성률 `2/2`, 다른 사람 폰은 `3/3`. 분모가 2와 3으로 기기마다 다름.
- **원인:** 코드의 `maxScore`는 v107부터 **2 하나뿐**(3점 경로 완전 제거). 따라서 `3/3`은 **옛 JS(family.js, maxScore=3)가 폰 HTTP 캐시에 남아** 로드된 것. → 순수 캐시 불일치.
- **왜 캐시가 안 빠졌나:** `index.html` 상단에 **서비스워커를 매 로드마다 해제 + 모든 캐시 삭제하는 kill-switch**가 있어 SW를 아예 안 씀. 그래서 sw.js CACHE_NAME을 올려도 무의미했고, JS는 오직 브라우저 HTTP 캐시로만 제어됐는데 TWA WebView가 옛 JS를 끈질기게 잡고 있었음.
- **근본 수정:** `index.html`의 모든 JS `<script src>`에 **버전 쿼리 `?v=109`** 부착. index.html은 `no-store`(항상 최신)라, 새 `?v`가 HTTP 캐시를 무조건 우회 → 모든 기기가 즉시 새 JS(maxScore=2)를 받음. SW 등록 여부와 무관하게 동작.
- **재발 방지:** 루트에 `bump-version.sh` 추가 — `bash bump-version.sh <버전>`으로 index.html `?v=`와 sw.js CACHE_NAME을 한 번에 올림. JS 수정 배포 때마다 실행하면 버전 불일치 원천 차단.
- **검증:** 배포된 `index.html`이 `?v=109` 참조 / `family.js?v=109`의 maxScore=2 / 캐시헤더 `max-age=0,must-revalidate` 확인.
- 파일: `app/index.html`, `bump-version.sh`(신규)

### 교구 현황 -2 (완료 6/2·300% 중복 집계 + 참가 인원 의미 정리)
- **증상1:** 1교구 `완료 6 / 참가 2 = 300%`. 완료 수가 참가(등록 인원)보다 큼.
- **원인1:** 완료(`byParish`)는 **deviceId(기기) 기준** 중복제거, 참가(`registeredByParish`)는 **이름 기준**. 한 사람이 여러 기기/브라우저(캐시삭제·시크릿모드 포함)로 완료하면 deviceId가 매번 달라 **같은 사람이 여러 번 카운트** → 완료 > 참가.
- **수정1:** `countCompletedMembersByParish`의 멤버 식별을 **이름 우선**(없을 때만 deviceId 폴백)으로 → 참가와 동일 기준, 같은 사람 1명. 추가로 `renderParishStatsFromAggregated`에 실천율 **100% 상한**(`Math.min(100,…)`).
- **증상2(오해):** "2·3교구는 참석 없는데 참가 2명씩 나옴".
- **설명2:** `참가`는 **참석 인원이 아니라 등록 인원**(분모). 2·3교구에 테스트 더미 가정(2교구 김민수·'3', 3교구 '대표'·'1')이 가족방 멤버로 **등록**돼 있어 2명씩 잡힘. 암송 완료와 무관. 이상한 이름('1','3','대표')은 테스트 입력이므로 관리자 데이터관리에서 해당 가정 삭제하면 정리됨(코드 문제 아님).
- 단위테스트: 6기록→이름기준 2명, 6/2→100%, 1/1→100%, 1/2→50% 통과.
- 파일: `functions/aggregate.js`, `app/js/memorize.js`, `app/sw.js`(v109)

### 교구 현황 -1 (교구 실천율 0/0·0% 집계 오류 수정 — 교구명 깨짐 + 분모 폴백)
- **증상:** 교구에 가정을 정상 등록했는데도 교구 현황이 `완료 0/0 · 참가 0 · 실천율 0%`로 전부 0.
- **진짜 원인(서버 데이터 손상):** 세광 `config/current.parishConfig.groups`가 **한글 인코딩 깨짐(mojibake)**으로 저장돼 있었음 (`"1교구"` → `"1占쏙옙占쏙옙"`, term `"교구"` → 깨진 한자). 서버 `countParishMembers`가 이 깨진 groups로 byParish 키를 만들어 → 가정의 정상 parish `"1교구"`와 `_matchGroup` 매칭 실패 → **모든 교구 참가/완료 0**. (가정·인원 데이터 자체는 정상: 3가정 6명)
- **수정1(데이터 복구):** saveConfig로 `parishConfig = {term:"교구", groups:["1교구","2교구","3교구","블레싱"]}` 정상 재저장 → registeredByParish 1·2·3교구 각 2명으로 정상 집계 확인.
- **2차 원인(분모):** `renderParishStatsFromAggregated`의 분모(total)가 **관리자 수동입력(`pat_admin_parish_edit`)에만 의존** → 미입력이면 분모 0 → 실천율 항상 0%.
- **수정2(분모 폴백):** 관리자 수동입력이 있으면 그 값(교인 전체 기준), **없으면 실제 등록 참가 인원(registeredByParish)으로 폴백** → "완료/참가" 기준 실천율 정상 표시. (예: 완료1/참가1 = 100%)
- **수정3(재발 방어):** mojibake 가드 추가 — `getParishConfig`/`applyCloudConfig`에서 교구명에 대체문자(`�`)나 CJK 한자가 섞이면 깨진 것으로 보고 캐시 삭제 후 기본값 폴백. 옛 폰 깨진 캐시도 자동 정리.
- **경로 검증:** 음성인식 완료 → `completeMemorize` → `saveRecord`(deviceId/familyId/parish/memberName/verseRef를 localStorage에서 채워 전송) → 서버 records 저장 → `getDashboard`의 `countCompletedMembersByParish`(deviceId 식별 + 가정 parish 매칭) → byParish 누적. **전 구간 정상 작동 확인**(단위테스트 통과).
- 파일: `functions`(서버 데이터 복구), `app/js/memorize.js`(분모 폴백), `app/js/app-core.js`(mojibake 가드), `app/sw.js`(v108)

### 홈 화면 -1 (오늘의 달성률 67% 막힘 오류 수정 — 통독 점수 제외)
- **증상:** 가족 2명이 암송+기도 모두 완료(가족방 "2/2명")했는데, 상단 **"오늘의 달성률"이 2/3점(67%)**로 막힘. 100%가 안 됨.
- **원인:** `updateHomeDisplay()`가 홈 달성률을 **암송/기도/통독 3가지(3점 만점)**로 계산. 그런데 **통독(읽기표)은 대한성서공회 API 미승인**으로 본문이 "API 승인 후 표시"만 나오는 형식적 기능 → 사실상 완료 의미 없음. 그런데도 분모(3점)에 포함되어 암송+기도를 다 해도 **최대 67%(2/3)**에 막힘.
- **불일치:** 가족방 미션 기준은 **암송 OR 기도 = 완료**인데, 홈 달성률만 통독을 포함해 기준이 어긋남.
- **수정:** 홈 달성률을 **암송/기도 2가지(2점 만점)**로 변경 → 둘 다 완료 시 2/2 = **100%**. 가족방 미션 기준과 일치. (통독 API 연동 시 `read` 변수와 `+(read?1:0)`/`maxScore 3` 복구하면 됨 — 주석으로 보존)
- 파일: `app/js/family.js`(updateHomeDisplay), `app/sw.js`(v107)

### 시상 관리 -1 (실천율 집계 오류 수정 — 0% 오류)
- **증상:** 가족이 암송 + 기도 모두 완료했는데, **한 명은 정상(20% 등) 표시, 다른 한 명은 0% 오류**.
- **원인:** `rankFamilies()` 함수가 계산할 멤버 목록을 `families.memberNames` **배열에만** 제한. 구성원이 완료 기록이 있어도 **초기 members에 등록되지 않으면** memberNames 배열에 없어 → 실천율 집계 대상에서 제외되어 0% 표시.
- **시나리오:** 
  - A가 암송(1개) + 기도(기도는 별도 집계) → memberNames=['A'] → A: 정상 계산 ✅
  - B가 기도 기록만 있음(초기 members에 미등록) → memberNames=['A'] → **B는 무시됨, 계산 대상 제외** → 0% 오류 ❌
- **수정:** `rankFamilies()` 멤버 목록을 Set으로 동적 구성:
  - 1) `f.memberNames` 배열에 있는 멤버 모두 추가
  - 2) `records` 데이터에 있지만 memberNames에 없는 멤버도 추가 (기록이 있으면 계산 대상에 포함)
  - 결과: members 배열에 미등록되었어도 기록만 있으면 실천율 정상 표시
- 파일: `functions/aggregate.js`(rankFamilies 함수), 배포: Cloud Functions v107

### 로그인/입장 -3 (혼합형 로그아웃 — 자동입장 + 로그아웃 시 비번 재요구)
- **요청:** "로그아웃이 완전하지 않아서" 비밀번호 없이 재입장됨 (보안 우려).
- **배경:** 기존(v105)은 "로그아웃 후에도 가족 데이터 영구 보존" → 다음 입장 시 자동 복구 → 비밀번호 입력 안 됨 (중복 생성 버그 회피 목적).
- **개선(혼합형):** 
  - **평소:** 앱을 닫았다 열면 → 저장 가족방으로 자동 입장 (편의)
  - **로그아웃:** 가족 데이터 완전 삭제 → 다음 입장 시 비밀번호 재입력 (보안)
  - 중복 생성 버그는 `loadFamilyProfile()`의 `pat_leader_family_profile` 백업 복구 로직이 해결 (데이터는 Firebase/서버에 유지, 로컬 백업으로도 1회 복구 가능)
- **수정:** `memberLogout()`에서 pat_family_profile/pat_family_id/pat_leader_family_profile 모두 삭제 + 토스트 "다음 입장 시 비밀번호를 입력해주세요" 추가.
- 파일: `app/js/app-core.js`, `app/sw.js`(v106)

### 로그인/입장 -2 (구성원이 교회코드로도 입장되는 우회 차단)
- **증상:** 대표가족은 가족 비밀번호로만 입장되는데, **구성원은 교회코드와 가족비번 둘 다로 입장**됨. → 교회코드 입장 우회 차단 필요(가족 비밀번호로만 입장).
- **원인:** `enterChurch()` 분기2 차단 조건이 `profile.familyPassword !== code` 뿐이라, 구성원의 **로컬 저장 비번이 교회코드와 같으면**(기본값으로 합류했거나 새 비번 미수령) 차단을 우회해 교회코드로 입장됐음.
- **진짜 원인(테스트로 발견):** 분기2 이전의 **분기1(`code===famPw`)이 로컬 저장 비번을 무검증 신뢰**. 구성원의 저장 비번이 옛 기본값(=교회코드)이면 교회코드 입력이 가족 비번으로 오인돼 **DB 검증 없이 먼저 입장**됨(분기2 도달 못 함).
- **수정:** ① 분기1에 `code !== DB.church.code` 추가 → 교회코드는 빠른 경로를 못 타고 항상 분기2로. ② 분기2에서 가족 소속 기기가 교회코드 입력 시 **DB로 '교회코드가 우리 가족 실제 현재 비번인지' 검증**(`findFamilyByPassword(교회코드,교회코드,내familyId)`) → 진짜일 때(레거시 기본비번 가족)만 허용, 아니면 차단하고 가족 비밀번호 요구. DB 미준비 시 저장 비번==교회코드 폴백. → 대표·구성원 모두 가족 비밀번호로만 입장.
- **검증(Playwright 모킹):** 구성원 교회코드 차단 / 구성원 가족비번 입장 / 신규(가족없음) 교회코드 입장 / 레거시 기본비번 가족 입장 — 모두 통과.
- 파일: `app/js/app-core.js`, `app/sw.js`(v105)

### 로그인/입장 -1 (가족 구성원이 가족 비밀번호로 입장 불가 수정)
- **증상:** 대표가족이 가족 비밀번호를 바꾼 뒤, **구성원은 새 가족 비밀번호로 입장 불가**. 엉뚱하게 교회코드를 입력해야만 입장됨.
- **원인:** `enterChurch()`(입장 화면)에 **입력값을 DB에서 가족 비밀번호로 조회하는 분기가 없음.** 로컬 저장 비번(`famPw`)과 정확히 일치할 때(분기1)만 통과 → 구성원 기기는 로컬에 '옛 비번'이 남아 있어 새 비번 입력 시 분기1 실패 → 나머지는 전부 교회코드로만 취급해 실패.
- **수정:** `enterChurch()`에 **가족 비밀번호 DB 조회 분기(1.5)** 추가. 교회가 선택돼 있으면 입력값으로 `findFamilyByPassword(교회코드, 입력값)` 호출 → 우리 가족방이면 **로컬 비번을 새 값으로 갱신하고 즉시 입장**. (교회코드 자체는 제외 → 보안 분기A 유지. `findFamilyByPassword`가 저장된 `pat_family_id` 기준으로 '내 가족'만 검증 → 타가족 무단열람 차단)
- **부가:** 이름이 없는 새 기기는 가족방 찾은 뒤 이름 입력으로 합류 유도.
- 파일: `app/js/app-core.js`, `app/sw.js`(v103)

### 기도 페이지 -1 (오늘 기도만 표시 + 자정 자동 초기화)
- **요청:** 기도 내용은 **오늘 하루만** 보여지고, **자정(00시)이 지나면 자동으로 사라져** 다음 날 다시 입력해야 보이는 구조. 단, **실천율/현황 데이터에는 과거 기록 보존**.
- **원인:** `todayKey()`가 `toISOString()`(UTC) 기준이라 한국시간 **오전 9시**에 날짜가 바뀜 → 자정~9시 사이엔 어제 기도가 그대로 노출됨.
- **수정1:** `todayKey()`를 **로컬(한국시간) 날짜** 기준으로 변경(`_localDateStr` 헬퍼 추가) → 자정에 정확히 전환. 화면은 항상 `today` 키만 읽으므로 어제 기도는 자동으로 안 보임(데이터는 localStorage에 날짜별로 보존 → 통계 유지).
- **수정2:** 홈 '오늘의 달성률' 암송 판정도 `completedAt`(UTC)을 **로컬 날짜로 환산**해 비교 → 자정~9시 사이 암송 누락 방지.
- **수정3:** 앱을 켜둔 채 자정을 넘겨도 기도 화면이 자동 새로고침되도록 30초 날짜 감시 추가(`_ensurePrayerDayWatch`).
- 파일: `app/js/family.js`, `app/js/prayer.js`, `app/sw.js`(v102)

## 2026-06-27

### 관리자 로그인 -2 (자동입장 방지 + 되돌리기 버튼)
- **증상1:** 앱을 새로 열면 캐시된 관리자 자격으로 **바로 관리자 화면**이 떠서(로그인된 채) 불편.
- **수정1:** 관리자는 **같은 세션 새로고침만 유지**(`pat_admin_session`), **앱 새로 열기(새 세션)면 로그인 화면**. 교인 가족방은 그대로 자동 입장. → 보안·"열면 로그인됨" 해소, 작업 중 새로고침은 안 튕김. `determineInitialScreen`/`completeAppInitialization`/`adminLogin`/`adminLogout`. 폴백도 관리자 단독은 중립(로그인 "PAT Bible").
- **증상2:** 관리자 모드에 뒤로가기/되돌리기 버튼이 없어 갇힘.
- **수정2:** 관리자 화면 좌상단에 **"‹ 처음으로"** 버튼 추가(→ 로그인).
- **검증(Playwright):** 새 세션=로그인 / 세션 새로고침=관리자 유지 / 가족=자동 / 되돌리기 버튼·동작 ✅
- 파일: `app/js/app-core.js`, `app/index.html`, `app/sw.js`(v77)

### 관리자 로그인 -1 (교회 코드 제거 — 아이디+비번만)
- **변경:** 관리자 로그인에서 **교회 코드 입력 제거** → **아이디+비밀번호만**으로 로그인. 관리자 아이디를 **전체 교회에서 유일**하게 만들어 아이디로 교회 자동 식별.
- **백엔드:** `adminIds/{소문자아이디}` 전역 인덱스. `registerChurch`가 아이디 중복(409)·예약어('admin') 차단 + 인덱스 생성. `adminLogin`이 {adminId,adminPw}로 인덱스 조회→교회 식별→검증. `checkAdminId`(중복확인). `deleteChurch`가 adminIds도 정리(아이디 영구점유 방지).
- **프론트:** 로그인 코드 필드 제거(`adminLogin` id+pw, 세광 admin/1234 유지). 등록 화면 아이디 **실시간 중복확인**(`checkRegAdminId`). `firebase-db.js` adminLogin(id,pw)·checkAdminId.
- **검증(curl+Playwright):** 등록·코드없는 로그인·중복409·예약어·틀린비번401·없는아이디404 / 세광 admin/1234 / 신규 아이디만으로 그 교회 로그인 / deleteChurch adminIds 정리(고아 복구) ✅
- 파일: `functions/index.js`, `app/index.html`, `app/js/app-core.js`, `app/firebase-db.js`, `app/sw.js`(v76)

### 교구/목장 설정 -1 (교회별 그룹 동적화 — 멀티교회 핵심)
- **목적:** 하드코딩된 세광 교구(1·2·3교구+블레싱)를 → **교회별로 호칭(교구/목장/구역/그룹)·그룹 이름·개수를 자유 설정**.
- **백엔드(동결 함수, 하위호환):** `aggregate.js` countParishMembers/countCompletedMembersByParish에 `groups` 인자 추가(없으면 1·2·3교구 레거시 = 세광 회귀 안전). 정확일치→normalize 폴백으로 옛 자유입력도 매칭. `getDashboard`가 `parishConfig.groups` 읽어 동적 집계 + 응답 포함. `saveConfig`가 `parishConfig` 저장.
- **프론트:** `getParishConfig()`(없으면 기본 교구4 폴백) · 관리자 "🧩 교구/목장 설정" UI(호칭+그룹 추가/수정/삭제) · 교구별 현황표 **동적 렌더**(p1/p2/p3 하드코딩 대체, 그룹키 parishTotals) · 가족 등록 교구 **드롭다운** · 성도 대시보드 동적. 블레싱=일반 그룹(명 단위)으로 통일.
- **세광 마이그레이션:** config에 `parishConfig=교구[1·2·3교구,블레싱]` + parishTotals 그룹키 변환(150/120/110/30).
- **검증:** 단위테스트(레거시=기존동일/동적/커스텀) · Playwright(세광 교구4·커스텀 목장3·드롭다운·그룹키저장·렌더 1/10=10%) · 백엔드(목장교회 새싹3) ✅
- 파일: `functions/aggregate.js`, `functions/index.js`, `app/index.html`, `app/js/{app-core,memorize,family}.js`, `app/firebase-db.js`

### 교회 정보 관리 -1 (앱제목 자동화 + 담임목사·주소)
- "앱 상단 제목" 입력/버튼 삭제 → **교회 이름 저장 시 앱 제목 자동 동기화**(로컬+서버 appTitle). **담임목사·주소** 입력란 추가(교회 정보 저장 버튼). 로그인 "새 교회 등록"→"교회 등록".
- 파일: `app/index.html`, `app/js/{verse,app-core}.js`

## 2026-06-26

### 로그인/시작 -2 (타 교회 설치 시 세광 브랜딩 제거)
- **증상:** 옛 버전을 띄운 적 있는 기기는 `pat_app_title='세광교회'`가 캐시로 남아, 중립시작 배포 후에도 로그인 첫 화면에 "세광교회"가 떠서 타 교회에 이질감.
- **수정:** `applyAppTitle()`이 **교회 미선택(`DB.church.code` 빈 값)일 땐 옛 캐시 무시하고 무조건 "PAT Bible"**. 교회 선택 후에만 그 교회 제목.
- **참고:** 앱 아이콘·이름(manifest)은 이미 "PAT Bible"(중립). 앱이 하나라 아이콘은 교회별 분리 불가.
- **검증(Playwright):** pat_app_title='세광교회' 캐시 + 교회 미선택 → "PAT Bible" / 교회 선택 → 그 교회명. 
- **배포:** **Firebase Hosting(pat-bible-app.web.app)** + GitHub Pages. sw.js v72→v73.
- 파일: `app/js/app-core.js`, `app/sw.js`

### 문의·오류 신고 -1 (접수 창구 + 자동 오류 수집)
- **목적:** 교회가 어디로 연락할지 고민 없이 앱에서 바로 신고 → 개발자가 한눈에 확인(접수 자동화).
- **백엔드:** `submitReport`(공개), `getReports`/`deleteReport`(개발자 토큰). `reports` 컬렉션.
- **프론트:**
  · 신고 화면 `s-report`(현재 화면·교회 자동 첨부) + 진입 버튼 3곳: 로그인 / 교인 홈 메뉴(hm-dropdown) / 관리자 헤더.
  · **자동 오류 수집:** `window.onerror`·`unhandledrejection` → submitReport(type:auto). 같은 메시지 1회·세션당 8건 상한(폭주 방지).
  · `firebase-db.js`: submitReport/getReports/deleteReport 래퍼.
  · **개발자 통계(dev-stats.html):** "📨 신고·오류" 목록(문의/자동오류 배지 · 교회 · 화면 · 시간 · 메시지 + ✓삭제).
- **검증(Playwright/curl):** 수동 신고 전송→적재, 자동수집 중복·상한 동작, dev-stats 목록 렌더(3건·배지), 삭제. 테스트 신고 정리 완료.
- **배포:** sw.js CACHE_NAME v71→v72.
- 파일: `functions/index.js`, `app/firebase-db.js`, `app/js/app-core.js`, `app/index.html`, `app/dev-stats.html`, `app/sw.js`

### 비밀번호 규칙 -1 (관리자 비번 = 특수문자→영문→숫자 순서, 8자+)
- **변경:** 관리자 비밀번호 규칙을 "숫자 8자리" → **"특수문자 → 영문 → 숫자 순서, 8자 이상"**(예: `#grace2026`)으로.
- **안내 문구:** 교회 등록 화면 비번 필드 아래 "🔒 특수문자 → 영문 → 숫자 순서로 8자 이상 (예: #grace2026)" 상시 표시 + placeholder.
- **검증:** 프론트(`registerChurchSubmit`)·백엔드(`ADMIN_PW_RE`) 동일 정규식 `^(?=.{8,}$)[^A-Za-z0-9]+[A-Za-z]+[0-9]+$`.
- **테스트(curl):** #grace2026 → 200 / 숫자만 → 400 / 순서틀림(grace#123) → 400 / 8자미만 → 400.
- 파일: `functions/index.js`, `app/index.html`, `app/js/app-core.js`, `app/sw.js`(v71)
- ※ 가족 비번/재설정 등 다른 비번 필드 적용 및 "개발자 사용자 확인" 범위는 추가 협의.

### 로그인/시작 -1 (중립 시작 — 멀티교회 SaaS)
- **변경:** 신규 방문자가 앱을 처음 열면 세광교회가 자동 표시되던 것을 → **교회 미선택(중립 "PAT Bible") 화면**으로. 교회는 코드 입력/관리자 로그인으로 선택.
- **구현(`app/js/app-core.js`):**
  · `DB.church` 기본값을 중립(`code:'', name:''`)으로.
  · `applyStoredData`: pat_church_code 있으면 그 교회, 없지만 가족/관리자 데이터 있으면(멀티교회 이전 기존 사용자) **세광 11111 폴백**(회귀 방지), 그 외 신규 방문자는 중립.
  · `initFirebase`: 교회 미선택 시 설정 로드 보류(세광 자동표시 방지). 설정 로드를 `loadChurchConfig()`로 추출.
  · `enterChurch`: 입력 코드를 서버 조회해 등록된 교회면 전환(세광 11111 포함). 구절 미등록 교회는 구절 비움.
  · `adminLogin`(세광/신규 모두): 로그인 후 해당 교회 설정 로드.
- **세광교회 유지:** 코드 11111 입력 또는 admin/1234 로그인 시 기존대로 세광(구절 포함). 기존 사용자는 폴백으로 그대로.
- **검증(Playwright):** 신규=중립("PAT Bible") / 11111→세광(베드로전서4장19절) / 신규교회 코드→자기 구절 / 기존 사용자 폴백 11111 / 세광 admin/1234 → 세광. 오류 0.
- **배포:** sw.js CACHE_NAME v69→v70.
- 파일: `app/js/app-core.js`, `app/sw.js`

### 개발자 통계 -1 (전 교회 사용 현황 + 교회 삭제 도구)
- **목적:** 여러 교회가 사용할 때 개발자가 "몇 개 교회·몇 명"을 한눈에 확인.
- **백엔드:** `getPlatformStats`(전 교회 집계 — `listDocuments`로 레거시 11111 포함, 교회별 가정·인원[멤버 배열∪서브컬렉션 합집합] 합산), `deleteChurch`(교회 재귀 삭제 — 테스트/스팸 정리). 둘 다 **개발자 토큰**(Firebase Secret `PAT_DEV_TOKEN`) 검증. 앱/코드에 토큰 노출 없음.
- **프론트(플랜 C = 둘 다):**
  · 별도 URL 페이지 `app/dev-stats.html` — 토큰 입력→요약(교회·가정·인원)+교회별 표+교회 삭제(확인 후).
  · 앱 내 숨김 화면 `s-dev-stats` — `?dev=1` 진입, 토큰 입력→요약+목록(`devStatsLoad`).
- **검증(curl+Playwright):** 토큰 없으면 401 / 정상 토큰 집계 / 별도 페이지·앱내 화면 렌더 / 테스트 교회 8개 삭제 후 세광 11111만 남음 확인.
- **정리:** 테스트로 생성된 교회(zz*, #100625, #901214, TEST_CHURCH, demo-church) 전부 삭제 → 현재 1교회(세광).
- 파일: `functions/index.js`(getPlatformStats, deleteChurch, PAT_DEV_TOKEN), `app/dev-stats.html`(신규), `app/index.html`, `app/js/app-core.js`, `app/sw.js`(v67)

### 기도 페이지 -1 (음성 인식 엔진을 암송과 통일)
- **변경:** 기도 음성 입력을 브라우저 Web Speech API(`SpeechRecognition`, ko-KR) → **암송과 동일한 Groq Whisper(클라우드)** 방식으로 통일.
- **이유:** 기존 Web Speech API는 카카오톡 인앱브라우저·일부 기기에서 미지원("음성 인식을 지원하지 않습니다"). Groq Whisper는 녹음 업로드 방식이라 구형폰·인앱브라우저에서도 동작.
- **구현(`app/js/prayer.js`):** `startPrayerMic`을 MediaRecorder 녹음 → `PAT_DB.transcribeAudio`(Groq) 전사 → 실패 시 `transcribeBlob`(브라우저 Whisper) 폴백 → 결과를 `prayerText`에 삽입·텍스트탭 전환. 90초 제한 타이머 유지. UI 레이아웃 동일(요소·버튼 그대로, 실시간 받아쓰기 → 종료 후 ~1~2초 인식으로 동작만 변경).
- **검증(Playwright, 합성 오디오+목):** 녹음 시작(recording, ⏹️) → 정지 → transcribeAudio 호출 → 텍스트 삽입·텍스트탭 전환 / Groq 실패 시 transcribeBlob 폴백 동작 ✅
- **배포:** `sw.js` CACHE_NAME v65→v66.
- 파일: `app/js/prayer.js`, `app/sw.js`

## 2026-06-25

### 가족방 -4 (재등록 중복 누적 근본 차단 + 기존 중복 정리)
- **증상:** 대표가 가족방을 다시 등록하면 **서버에 가정 문서가 중복 생성**(교회 등록 인원에 잔류). 11111 권호택/예운네가 134·135 등 **3개까지 누적**.
- **원인:** familyId 분실(카톡 인앱브라우저/다기기/이중제출) 상태에서 재등록하면 `saveFamily` 생성 경로가 무조건 새 문서 `add` → 포크. 서버에 중복 방지 없음.
- **수정(서버 `saveFamily`):** 생성 시 **같은 (대표+교구)** 기존 가정이 있으면 새로 만들지 않고 그 문서를 갱신(구역=최신값, 멤버=합집합). 응답 `{familyId, merged:true}` → 클라이언트가 그 id를 저장해 **familyId 자동 복구**(재포크 차단). 합의 기준: 대표+교구 동일 시 동일 가정(구역 무시).
- **신규 `deleteFamily`(관리자 전용):** family 문서 + members 서브컬렉션 + 해당 records 정리. 교회별 admin 인증(레거시 11111 전역 폴백).
- **기존 11111 정리:** 권호택 중복 3개 중 기록 보유 `Ktsr…`(예운네/135/100%) 보존, 빈 2개(`rhnd…`,`I7UX…`, 기록 0건) 삭제 → **1가정·1명**으로 정정(대시보드 registeredByParish 1교구=1, completed=1 확인).
- **검증(curl):** 격리교회에서 familyId 없이 같은 대표+교구 재등록 → `merged:true` 동일 familyId, 구역 최신화, 멤버 합집합 / 다른 교구는 별개 생성 / getFamiliesList 총 2(포크 아님). 11111 삭제 후 1가정.
- 파일: `functions/index.js`(saveFamily dedup, deleteFamily)

### 멀티 교회 -1 (교회별 독립 등록·로그인·격리)
- **목적:** 하드코딩된 단일 교회(세광/11111, admin/1234)에서 → **누구나 교회를 셀프 등록**해 독립 운영.
- **데이터 모델:** `churches/{코드}/admin/cred`{adminId, adminPwHash}, `churches/{코드}` 루트{name}. 데이터는 기존처럼 교회코드로 네임스페이스.
- **백엔드(신규/수정):**
  · `registerChurch`(오픈 셀프 등록, 기존 교회 코드 덮어쓰기 409 차단)
  · `adminLogin`(교회코드+아이디+비번 대조)
  · `checkChurchCode`(코드 중복확인)
  · `saveVerse`/`saveConfig`: 전역 토큰 → **교회별 admin 검증**(`assertChurchAdmin`). admin 문서 없는 레거시(11111)는 전역 폴백 → **세광 회귀 유지**.
  · `security.js` `validChurchCode`에 안전 특수문자(`# @ * ! - _`) 허용. **전 함수 재배포**로 모든 엔드포인트에 적용.
- **코드 형식:** 교회코드=특수문자1+숫자6(예 `#482913`), 관리자 아이디=영문3~20, 관리자 비번=숫자8.
- **프론트:** 교회 등록 화면(s-church-register, 단계 안내+중복확인), 관리자 로그인에 교회코드 필드, `DB.church.code` 동적화(`pat_church_code`), 교인 입장 시 신규형식 코드면 교회 전환(`enterChurch`), `adoptChurch`/`registerChurchSubmit`/`checkRegChurchCode`.
- **세광교회:** 테스트 기간 현행 유지(코드 비움+admin/1234 → 11111). 추후 동일 방식 전환.
- **검증(curl+Playwright):**
  · 백엔드: 세광11111+레거시토큰 saveConfig 200(회귀) / 무인증 401 / 신규 등록 ok / 신규 관리자로그인 ok / 본인 id·pw saveConfig 200 / 틀린비번 401 / **타교회 전역토큰 401(격리)** / 재등록 409 / 형식위반 400 / 코드중복확인 ok
  · 프론트: 교회 등록→자동로그인→은혜교회 진입 / 세광 관리자(코드비움+admin/1234) / 신규 관리자(코드+id+pw) / 틀린비번 거부 / 교인 코드 입장 전환
- 파일: `functions/index.js`, `functions/security.js`, `app/firebase-db.js`, `app/js/app-core.js`, `app/index.html`, `app/sw.js`(v65), `docs/guides/멀티교회-사용가이드.md`

### 홈/라우팅 -2 (로그아웃 후 새로고침 시 이전 화면 튕김 수정)
- **증상:** 로그아웃 후 페이지를 당겨서 새로고침하면 로그인 화면이 아니라 **이전 화면(가족/관리자)으로 튕김**.
- **원인:** 새로고침 시 초기화 로직은 `pat_stay_login`(sessionStorage) 플래그가 있으면 로그인 유지하는데,
  이 플래그는 `go('s-login')`을 통할 때만 설정됨. **로그아웃 함수는 `go()`를 거치지 않고 DOM을 직접 조작** →
  플래그 미설정. 게다가 `memberLogout`은 localStorage만 지우고 `loadFamilyProfile()`이 읽는
  **sessionStorage 백업은 미삭제** → 남은 자격(관리자 id/pw)·백업으로 이전 화면 복원.
  · 재현(Playwright): member 로그아웃(관리자 자격 잔존) → 새로고침 → `#s-admin`으로 튕김 확인.
- **수정:** `memberLogout`/`adminLogout` 둘 다 **`sessionStorage.setItem('pat_stay_login','1')`** 설정
  (+ `memberLogout`은 `sessionStorage` 가족 백업도 제거). 초기화 로직이 무조건 로그인 유지.
- **검증(Playwright, 실제 파일 로드):**
  · A: member 로그아웃(관리자 잔존) → 새로고침 `s-login` ✅ (수정 전 `s-admin`)
  · B: admin 로그아웃(가족 잔존) → `s-login` ✅ (수정 전 `s-family`)
  · C: 재로그인 시 `go('s-family')`가 플래그 해제 → 정상 입장(잠금 없음) ✅
  · end-to-end 실제 새로고침: 로그아웃 후 `#s-admin` 미발생, `active=s-login` ✅
- **배포:** JS 3종 변경 반영 위해 `sw.js` `CACHE_NAME` v64→v65 (안 올리면 사용자가 옛 JS 계속 사용).
- 파일: `app/js/app-core.js`(memberLogout/adminLogout), `app/sw.js`

### 가족방 -3 (구성원 삭제 동기화 — 합집합 부활 버그 수정)
- **증상:** 대표가 구성원을 삭제해도 1초 폴링 후 다시 나타남(특히 초대 링크로 실제 입장한 멤버).
- **원인:** 백엔드 `getFamilyProgress`가 **members 배열 ∪ members 서브컬렉션**(입장기록) 합집합을 반환.
  `deleteFamilyMember`는 `saveFamily`로 **배열만** 덮어쓰고 `joinFamily`가 만든 서브컬렉션 doc은 남김
  → 합집합에서 부활.
- **수정:** 삭제 시 **배열 + 서브컬렉션을 함께 정리**.
  · 백엔드 신규 `removeFamilyMember`: members 배열에서 이름 제거 + 같은 이름의 서브컬렉션 입장기록 삭제
  · 프론트 `firebase-db.js`: `removeFamilyMember` 래퍼 + export
  · 프론트 `deleteFamilyMember`(async): 새 엔드포인트 호출, **실패/미배포 시 기존 `saveFamily` 폴백**(배열만 갱신 — 회귀 방지)
- **배포 필요:** `firebase deploy --only functions`(removeFamilyMember). 미배포 시 폴백으로 기존 동작 유지(부활 버그만 잔존).
- **검증 필요(2기기):** 대표가 초대로 입장한 멤버 삭제 → 두 기기 모두에서 사라지고 폴링 후 부활 안 함.
- 파일: `functions/index.js`(removeFamilyMember), `app/firebase-db.js`, `app/js/family.js`

## 2026-06-24

### 등록가정 -1 (관리자 신규 탭) + 테스트 데이터 초기화
- **테스트 중복 가족 초기화:** 교회(11111)의 테스트 등록 **15가정·21멤버·1기록 전체 삭제** → 클린 슬레이트.
- **신규 탭 "📋 등록가정"** 추가 (시상관리 옆): **표(행열) 형식** — 컬럼 `번호·교구·구역·가족 이름(대표)·참여 인원`. 상단 요약 "총 N가정·M명".
  · 등록 즉시 교구별 현황 '참가'에 반영(동일 Firestore 소스). 5개 탭 전환 5/5 검증.
  · 백엔드 `getFamiliesList`(가족+멤버 합집합 인원)
  · 프론트 `renderFamiliesList` + `switchAdminTab('families')` + 탭 버튼/콘텐츠
  · 상단 요약: "총 N가정 · M명"
- **검증:** 탭 5개 순서(구절·교회·시상·등록가정·비번), 모의 2가정 → "총 2가정 · 5명" + 가족별 인원 표시 ✅
- 파일: `functions/index.js`(getFamiliesList), `app/firebase-db.js`, `app/js/admin.js`, `app/index.html`

### 시상관리 -1 (가족별 실천율 — Firestore 통합)
- **증상:** 시상관리가 교구별 현황과 따로 놀아 확인 불가. `getRankedFamilies`가 미완성 스텁
  (로컬 본인 가족 1개만, 존재하지 않는 `pat_rec_{날짜}` 키 사용, `profile.familyId`는 없는 필드 → 항상 빈/0).
- **수정:** 시상관리를 **교구별 현황과 동일한 Firestore 소스로 통합**.
  · 백엔드 `getAwardRanking`: 교회 전체 가족·멤버(선언+입장)·기록·총구절수로 **가족별 실천율 순위** 계산
  · 실천율 = 멤버가 완료한 구절 수 / 총 구절 수, 가족 평균. 순수함수 `rankFamilies`(aggregate.js, 단위테스트)
  · 프론트 `getRankedFamilies`/`searchFamilyForAward` → 서버 데이터 사용(UI 포맷 유지)
- **검증:**
  · 단위테스트: 김믿음가족 75% 1등 등 ✅
  · 라이브: 교회 15가족 전원 표시(실제 이름·멤버) ✅
  · **end-to-end: 테스트가족 완료 기록 → 시상에 실천율 100% 즉시 반영** ✅ (테스트데이터 정리 완료)
- 파일: `functions/index.js`, `functions/aggregate.js`(rankFamilies), `app/firebase-db.js`, `app/js/admin.js`
- 참고: 교구별 현황(완료/전체)은 관리자↔대시보드 이미 일치. 대시보드만 '참가'(등록인원) 추가 표시 — 포맷 유지로 관리자엔 미추가.

## 2026-06-23

### 가족방 -2 (등록 후 초기화 — 재발 버그 근본 수정)
- **증상:** 대표가 가족방(이름·대표·교구·구역) 등록 후 또 초기화로 돌아감.
- **원인:** `startFamilyProgressPolling`(1초 동기화)이 서버에서 **빈 값**을 받으면
  (가족 doc 누락·타이밍·일시 오류) 조건이 `!== undefined`라 **빈 값으로 로컬 등록정보를 덮어써 삭제**.
  → roomName/leaderName/parish/district가 지워져 "등록 안 됨" 상태로 보임.
- **수정:** 서버 값이 **있을 때만**(truthy) 갱신 → 빈 응답이 로컬 데이터를 지우지 못함.
- **검증:** 불완전 familyId로도 재로딩 시 "믿음가족 / 대표 김대표 1교구 3구역" 유지(로컬·라이브).
  (수정 전엔 "김대표네 가족 / 김대표"로 초기화됐음)
- 파일: `app/js/family.js`

### 가족방 -1 (오류 2건 수정)
- **Bug1: 등록만 해도 이번주 달성률 N/N(완료)로 잡힘 (발생 안 한 데이터 반영)**
  - 원인: `renderFamilyMemberList`가 완료수=인원수, 100% 하드코딩 + 멤버 전원 "✓ 완료" 표시.
    백엔드 `getFamilyProgress`도 `done:true` 고정. `renderFamily`도 `done:true` 하드코딩.
  - 수정: 완료 = **현재 구절 실제 기록**으로 판정.
    · 백엔드 `getFamilyProgress`: records(familyId+verseRef)로 멤버별 done 계산
    · `renderFamilyMemberList`: `filter(done)`로 카운트 + 완료/미완료 표시
    · `renderFamily`: 본인은 로컬 기록, 나머지는 미완료(클라우드 동기화로 보정)
    · `DB.members` 데모 기본값 done:false
- **Bug2: 대표 등록 후 가족방이 자꾸 초기화**
  - 원인: `refreshFamilyProfileByPassword`가 매 렌더마다 비번으로 가족 조회 →
    기본 비번이 교회코드로 공유 → familyId 확정 전 **다른 가족이 매칭되어 프로필 덮어씀**.
  - 수정: familyId 확정된 경우에만, 반환 가족이 내 familyId와 일치할 때만 갱신.
- 테스트: 등록직후 0/2명·미완료, 본인완료 1/2명, 초기화 차단 (6/6 통과)
- 파일: `functions/index.js`(getFamilyProgress), `app/js/family.js`, `app/js/app-core.js`

## 2026-06-22

### 홈/라우팅 -1 (중대 오류 수정)
- **증상:** 홈(성도) 화면에서 새로고침하면 관리자 화면으로 튕김.
- **원인:** `completeAppInitialization`이 가족 판정을 `savedFamily.familyId`로 했는데,
  `familyId`는 프로필 객체가 아니라 별도 키 `pat_family_id`에 저장됨 → 조건이 항상 거짓 →
  관리자 자격(`pat_admin_id/pw`)이 저장된 기기에서 가족이 있어도 `s-admin`으로 덮어씀.
- **수정:** 가족 판정을 `determineInitialScreen`과 동일하게 "프로필 존재"만으로 → `s-family` 우선.
- **테스트:** 가족+관리자이력→홈, 미동기화가족+관리자→홈, 관리자만→관리자, 초기→로그인 (4/4 통과)
- 파일: `app/js/app-core.js`

### 음성 암송 페이지 -6 (Groq 클라우드 전환 — 1~2초)
- 브라우저 Whisper(4초~폰 수십초) → **Groq Whisper large-v3-turbo(클라우드)** 전환.
  · 폰은 녹음만 업로드, 서버 GPU가 전사 → **~1~2초** (구형폰도 동일)
- 구성: 백엔드 `transcribeAudio`(Groq 프록시, 키는 Firebase Secret `GROQ_API_KEY`),
  프론트 `groq-asr.js`(`groqToggleMic`) + `GROQ_ENABLED=true`, 실패 시 Whisper 자동 폴백.
- 검증(라이브): 함수 직접 1.7초, 브라우저→Groq **1.2초**, 전사 정확(자모/포함 채점 100%).
- ⚠️ 후속: `transcribeAudio`는 현재 cors-only(무인증) — 남용 방지용 토큰 검증 추가 권장.
- 파일: `functions/index.js`, `app/firebase-db.js`, `app/js/groq-asr.js`, `app/index.html`

### 음성 암송 페이지 -5 (모델 등급 검토 → base 유지 확정)
- 상위 모델 실측(WASM·데스크톱 4스레드):
  · whisper-tiny: 2.5초지만 오인식("여호와는"→"영어하는"), 정확도 borderline 90% → 제외
  · whisper-large-v3-turbo: **728MB·로드93초·전사68초** → 사용 불가
  · large-v3(풀): turbo보다 더 큼/느림(디코더 32층) → 더 불가
- 결론: 브라우저 내장(무료·오프라인) 조건에서 **`whisper-base`가 최적** → 유지.
  (turbo급은 GPU 전용 → 원하면 클라우드 STT만 현실적: API키·비용·네트워크 필요)
- 모델 미변경(라이브 그대로 base). 코드 `WHISPER_DTYPE` 상수만 정리.

### 교구별 현황 -1 (시뮬레이션 형식 반영)
- 교구별 현황에 **참가인원** 추가 + 라벨 **진도표→실천표**, % = 실천율.
  · 백엔드 `getDashboard`가 `registeredByParish`(교구별 등록 인원) 반환
  · `renderParishStatsFromAggregated`에 참가인원 표시(`완료 N/전체 · 참가 N`)
- 파일: `functions/index.js`, `app/js/memorize.js`, `app/index.html`

### 음성 암송 페이지 -4 (대기시간 단축 — 반응 속도 개선)
- **증상:** 정확도는 나오나 전사 대기시간이 김(단일스레드 ~9초).
- **원인:** WASM이 **1스레드**로 동작(교차출처 격리 미설정 → SharedArrayBuffer 불가).
  (포트 8000을 Python 서버가 점유해 로컬 헤더 적용도 안 됐던 점도 발견)
- **수정:** **교차출처 격리(COOP/COEP) 헤더 추가** → WASM **멀티스레드(4코어)** 활성화.
  · `firebase.json`에 `Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: credentialless`
  · `server.js`(로컬)에도 동일 헤더
- **검증(라이브):** crossOriginIsolated=true, threads=4, 전사 **9초→4.1초(2.3배↑)**, 정확도 유지.
  Firebase(getConfig·getDashboard)도 COEP에서 정상 동작 확인(생산 안전).
- 참고: whisper-tiny(2.5초)도 검토했으나 정확도 borderline(90%)이라 **base 유지**.
- 파일: `firebase.json`, `server.js`, `app/js/whisper-asr.js`(멀티스레드 설정)

### 음성 암송 페이지 -3 (실기기 성능 문제 해결)
- **증상:** Whisper 교체 후에도 마이크 "성능 개선 안 됨".
- **직접 테스트로 찾은 원인 3가지:**
  1. **WebGPU 추론이 행(hang)** — 모델은 로드되나 전사가 무한 대기 → 결과 없음.
     → **WASM(안정)으로 전환**(WebGPU 미사용). WASM 멀티스레드 설정(교차격리 없으면 1스레드).
  2. **whisper-small이 너무 느림(40초+)** → **whisper-base로 교체**(전사 ~11초).
  3. **채점이 여분/반복 발화에 취약** — Whisper가 침묵 구간에 문장을 반복 생성하면
     edit-distance가 길이차로 급락(예: 23%).
     → **포함(containment) 채점 추가**(`containmentSimilarity`): 구절이 전사 안에 들어있으면
     앞/뒤 여분·반복을 무시하고 ~100% 인정. 반복 억제 `no_repeat_ngram_size`도 추가.
- **검증:** 가짜오디오 전체 흐름(녹음→Whisper→채점) — 전사 정확, 올바른 구절 대비 **유사도 100%**.
  (테스트 중 23%는 config 폴링이 DB.verse를 라이브 구절로 덮어쓴 하네스 artifact로 확인)
- 파일: `app/js/whisper-asr.js`(base·WASM·반복억제), `app/js/voice.js`(containmentSimilarity), `app/js/voice-ui.js`(채점)

### 음성 암송 페이지 -2
- **정확도 향상 → 유사도 100% 달성**
- ① 채점에 **자모(음소) 단위 비교** 추가(`jamoNormalize`): "목자시니"↔"목자신이"처럼
  ASR 재음절화·무음 초성 ㅇ 차이를 음소 레벨에서 동일하게 인정 (90%→**100%**)
- ② Whisper 모델 `whisper-base` → **`whisper-small`** (한국어 정확도↑)
- 테스트: TTS 한국어 음성 → 자모 유사도 **100%** 확인(헤드리스)
- 파일: `app/js/voice.js`(jamoNormalize), `app/js/voice-ui.js`(previewVoice 채점), `app/js/whisper-asr.js`(모델)
- 참고: small은 최초 다운로드가 큼(약 70초, 이후 캐시). 구형폰 부담 크면 `whisper-asr.js`의 `WHISPER_MODEL`을 base로 한 줄 변경

### 음성 암송 페이지 -1
- 마이크 음성인식을 **Web SpeechRecognition → 브라우저 내장 Whisper(transformers.js)** 로 교체
  (한국어 인식 오류 감소 목적). 모델 `Xenova/whisper-base`(q8), 최초 1회 다운로드 후 캐시
- 흐름: 녹음(MediaRecorder) → Whisper 변환 → 기존 채점(`evalVoice`) 재사용
- 마이크 버튼 `toggleMic()` → `whisperToggleMic()`, 음성 화면 진입 시 모델 프리로드
- 테스트: 한국어 TTS 음성 전사 글자 90% 일치 확인 (헤드리스)
- 파일: `app/js/whisper-asr.js`(신규), `app/index.html`
- 참고: 기존 `voice.js`의 SpeechRecognition 엔진 코드는 미사용(데드)으로 남김(헬퍼는 재사용)

### 관리자 -1
- 🧭 교구별 현황 **관리 편집 폼** 추가: 완료=자동 집계(읽기전용), 전체 인원=관리자 입력
- 저장/복원 일원화(중복·죽은 코드 제거), 관리자↔대시보드 동기화 수정
- **기준 날짜 → 적용 주차·기간 자동 연동** 버그 수정(저장된 옛 주차로 덮어쓰지 않음)
- 전체 인원을 **서버(config.parishTotals)에도 저장** → 전 성도 기기와 동기화
- 파일: `app/index.html`, `app/js/app-core.js`

### 현황 대시보드(성도) -1
- 교구별 현황 = **현재 구절 진도(완료 멤버 수)** 로 표시 (진도 없으면 0 — 가짜 숫자 제거)
- 분모(전체 인원)는 관리자 설정값을 서버에서 받아 동기화
- 변경 감지에 관리자 인원 포함 → 관리자 변경 즉시 반영
- 파일: `app/js/memorize.js`

### 가족방 등록 페이지 -1
- 교구 입력을 **자유 텍스트 → 드롭다운(1교구/2교구/3교구)** 으로 변경
  (옛 자유입력 "1", "1 교구" 등이 집계 누락되던 문제 해결)
- 파일: `app/index.html`

### 백엔드(Functions) -1
- `getDashboard`: 교구별 = **완료한 멤버 수** 집계(멤버 단위 중복제거) + 교구값 정규화(레거시 "1"→"1교구")
- `getDashboard`: 등록 인원 헤드카운트(`totalMembers`) 추가
- `saveConfig`: **부분 업데이트** 지원(교구 인원만 저장 시 구절/제목 보존), `parishTotals` 저장
- 파일: `functions/index.js`, `functions/aggregate.js`(신규)

### 공통/DB -1
- `firebase-db.saveConfig(...)`에 `parishTotals` 전달
- `applyCloudConfig`: 서버 `parishTotals`를 모든 기기 localStorage로 미러링(단일 진실 소스)
- 파일: `app/firebase-db.js`, `app/js/app-core.js`

---

_배포: Firebase Functions + Hosting (https://pat-bible-app.web.app)_

### 음성 암송 페이지 -7 (통과 기준 85%)
- 일반 음성 통과 기준 90% → **85%** (`TH().voice`). 표시 "통과 기준 N%"도 자동 반영. 관대모드 80% 유지.
- 폼/레이아웃 변경 없음. 파일: `app/js/app-core.js`

### 관리자 교구별 현황 관리 -2 (디자인 정렬)
- 명시적 요청으로 UI만 개선: 교구별 카드를 stat 카드 형태로 정렬
  (큰 % + 완료 박스 / 전체 인원 입력 박스, 블레싱은 accent 상단선).
- 입력 ID·`updateParishPct`·`saveAdminParishData` 등 기능/JS 전부 보존(동결 함수 미변경).
- 검증: ID 7개 존재, 완료 자동집계 30·pct 20%·저장 정상 + 스크린샷 확인.
- 파일: `app/index.html`

### 관리자 교구별 현황 관리 -3 (등록가정 표 형식)
- 요청대로 교구별 현황 UI를 **등록가정과 동일한 격자 테이블**로 교체.
  컬럼: `교구 · 완료 · 전체 인원(입력) · 실천율`, 셀 테두리, 블레싱 행 accent 상단선.
- 입력 ID·`updateParishPct`·`saveAdminParishData` 전부 보존(동결 함수 미변경).
- 검증: ID 7개·완료 자동집계 30·pct 20%·저장 150 정상 + 스크린샷 확인.
- 파일: `app/index.html`
---
## v115 (2026-06-30) — 데이터 초기화 재발 방지 하드닝
- **?reset 데이터 보존(app-core.js)**: 데모 캐시 리셋이 미션/기도/통독/암송/기기ID 등
  사용자 데이터를 프리픽스 기반으로 보존(삭제 금지). 임시 UI 키만 제거.
- **개인 실천율 서버 재동기화(functions getFamilyProgress + family.js)**:
  getFamilyProgress에 doneToday(KST 오늘 완료) 추가. 홈에서 서버가 '오늘 완료' 확인 시
  로컬 pat_records에 오늘 기록이 없을 때만 add-only로 채움(기기교체/재설치/멀티폰 0점 오표시 방지).
- 검증: getFamilyProgress 라이브 호출 시 권호택·엄마현 done=true, doneToday=true 확인.
  (curl NFD 정규화로 false였던 건 테스트 artifact — 앱은 NFC 사용으로 정상)
- hasRecord는 클라 미사용(죽은 코드) → ② 미적용. JS ?v=115, sw.js v115.
## 2026-07-01 가족 비밀번호 숫자 5자리 로그인 오류 수정

- 증상: 로그인 페이지에서 교회 코드 `11111` 입력 후 `이미 가족이 등록되어 있습니다. 가족 비밀번호로 입장해주세요.` 안내가 나온 뒤, 가족 비밀번호 `22222` 같은 숫자 5자리 값을 입력하면 가족 인증이 아니라 새 교회 코드 검증으로 처리될 수 있었다.
- 원인: `enterChurch()`가 교회 선택 완료 상태에서도 `/^\d{5,}$/` 형식이면 입력값을 새 교회 코드로 오인해 `DB.church.code`를 초기화했다.
- 수정: 교회가 이미 선택된 비밀번호 입력 단계에서는 숫자 5자리라도 가족 비밀번호로만 처리한다. 다른 교회 선택은 기존 `다른 교회` 초기화 버튼으로 수행한다.
- 검증: `tests/family-login-flow.test.cjs` 추가. `11111` 선택 → 기존 가족 안내 → `22222` 입력 시 `findFamilyByPassword('11111','22222')`가 호출되고 교회 코드 오류가 나오지 않음을 확인했다.
---
## v161 (2026-07-02) — 운영 안정성 마무리 점검 + 재발방지 하드닝 배포
- **범위**: 로그인/가족방/데이터/삭제/기록보존 전면 점검. 새 기능 없음, 원인 로직만 검증·보강.
- **로그인(app/js/app-core.js)**: `enterChurch()` 중복 제출 가드(`_enterChurchInFlight`) 추가 —
  버튼 클릭+Enter 동시 실행으로 "이미 가족이 등록" 토스트가 중복되던 문제 차단.
  신규 교회 첫 교인(가족 0개)일 때 비번 입력 막다른 길 대신 대표 등록 화면으로 안내.
- **가족 등록(app/js/family.js)**: `saveFamilyProfileAsLeader` 필수검증에서 `district` 제거 —
  구역방 삭제로 hidden·빈값이 된 뒤 모든 신규 가족 등록이 100% 실패하던 치명 버그 수정.
- **백엔드(functions/index.js)**: `saveFamily`가 `familyPassword===churchCode` 저장을 서버에서 차단
  (교회코드로 입장 가능한 방 생성 원천 봉쇄). `deleteFamily`에서 records 삭제 로직 제거 —
  향후 "가족방 삭제" 버튼 추가 시에도 미션 수행 기록(시상 근거) 보존.
- **테스트(tests/family-login-auth.test.cjs)**: 제거된 `REJECT_CHURCHCODE` 기대를 현재 설계
  (판정 통과 + resolve/서버 차단)로 갱신 → 교회코드 차단 가드 녹색 복원.
- **캐시버스트**: index.html `?v=139`→`?v=161`(21곳), sw.js 헤더 v161. sw.js는 무캐시 패스스루 유지.
- **데이터 보존 확인**: `pat_hist_<날짜>` 누적·무삭제, 서버 records 권위저장소(캐시삭제/기기변경 재조회),
  `?reset=1` 사용자 데이터 전량 보존, 날짜변경 시 진행세션만 초기화(voice.js v117 원칙 유지).
- 검증: 테스트 28개 중 핵심 재발방지 가드 전부 PASS(family-login-auth, family-delete-preserves-records,
  functions-password/security, mission-history). 잔여 실패 13개는 삭제된 기능(rooms)·임계값 변경 등
  낡은 테스트로 앱 회귀 아님(HEAD 대비 stash 비교로 확인).
---
## v170 (2026-07-03) — 전세대 친화 디자인 리파인 (폰트·색상·로고·메뉴)
- **범위**: UI 스타일만 개선. 폼/레이아웃/로직/데이터 구조는 그대로 유지(폼 유지 규칙 준수).
- **폰트(app/index.html)**: 시스템 `Malgun Gothic` → **Pretendard Variable**(CDN 임포트) 교체.
  `-webkit-font-smoothing:antialiased` + `letter-spacing:-0.01em`로 한글 가독성·선명도 향상.
- **색상 토큰(:root)**: 배경 `#e8ecf1`→`#f7f9fc`(웜화이트), surface `#f8fafb`→`#ffffff`,
  액센트 `#00a854`(형광초록)→`#0f7a52`(딥그린, 신뢰+선명), muted `#5f6b7c`→`#4b5563`(대비↑),
  line `#d9dfe8`→`#e2e8f0`, danger `#ef4444`→`#dc2626`. `--accent-soft:#e3f3ec` 신규(선택탭 배경).
  theme-color 메타도 `#f7f9fc`로 동기화. WCAG 본문 15.8:1 / 보조 8.1:1 (AAA).
- **로고(로그인 헤더)**: `pat-logo.png` 이미지 → **인라인 SVG 심볼(책+틸) + 워드마크 "PAT Bible"**
  ('Bible'만 딥그린 800 강조). `applyAppTitle()`가 기본명은 innerHTML로 굵게, 교회 커스텀명은
  textContent로 표기하도록 수정(app/js/app-core.js) — SVG 마크 유지하면서 교회별 명칭 지원.
- **탭바 메뉴**: 선택 탭에 **연녹 알약 배경(--accent-soft)** + 아이콘 20→23px, 라벨 굵기 600,
  세로 정렬·터치영역 확대·상단 그림자·safe-area 패딩 추가 → 어르신 현재 위치 즉시 인지.
- **캐시버스트**: index.html `?v=169`→`?v=170`(21곳), sw.js 헤더 v170, data-version 갱신.
  sw.js는 무캐시 패스스루 유지(CACHE_NAME 없음).
- **배포**: firebase deploy (pat-bible-app → web.app).
