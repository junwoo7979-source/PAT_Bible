# PAT Bible 테스트 가이드

**목적**: Google Play 등록 전 완벽한 테스트를 통해 오류 없는 앱 제출

---

## 📱 테스트 환경 설정

### 필수 기기
- Android 12 이상 스마트폰 (최신 버전 권장)
- 마이크가 정상 작동하는 기기

### 테스트 방법
1. **웹 버전**: https://pat-bible-app.web.app (Chrome/Firefox)
2. **TWA 앱**: Google Play (테스트용 APK 설치)

---

## ✅ 기능 테스트 체크리스트

### 1️⃣ 로그인 & 교회 입장

```
[ ] 앱 아이콘 터치 → 로그인 화면 깜빡임 없이 표시
[ ] 교회 코드 입력 (테스트: 11111)
[ ] "시작하기" 버튼 클릭
[ ] 가족방 목록 화면으로 이동
[ ] 뒤로가기 → 로그인 화면으로 복귀 (금지됨 확인)
```

### 2️⃣ 가족방 기능

```
[ ] 가족방 목록 표시 정상
[ ] 각 가족방 진행도 표시 정상
[ ] 가족방 입장 (새로 등록 또는 기존)
[ ] 가족 구성원 목록 표시
[ ] 구성원 입장 완료/대기 상태 표시
[ ] 실시간 동기화 작동 (다른 기기에서 추가 시 감지)
```

### 3️⃣ 미션 암송 (음성 인식)

```
[ ] "암송 시작" 버튼 클릭
[ ] 마이크 권한 요청 팝업 → "허용" 선택
[ ] 구절 표시 정상
[ ] 마이크 버튼 표시
[ ] 마이크 버튼 누르면 녹음 시작 (빨간색 + 펄스)
[ ] 성경 구절 음성 입력
[ ] 마이크 버튼 놓으면 녹음 종료
[ ] 음성 인식 결과 표시 (녹색/빨강 색상 코딩)
[ ] 유사도 백분율 표시
[ ] 100% 통과 시 "다음" 버튼 활성화
[ ] 단계별 진행도 업데이트
```

### 4️⃣ 타이핑 검증

```
[ ] "타이핑으로 검증" 탭 선택
[ ] 구절 입력 필드 표시
[ ] 타이핑 입력
[ ] 진행률 바 업데이트
[ ] 100% 입력 시 통과
[ ] 진행도 업데이트
```

### 5️⃣ 완료 화면

```
[ ] 모든 단계 완료 시 완료 화면 표시
[ ] 4단계 결과 표시 (각각의 점수)
[ ] "확인" 버튼 클릭
[ ] 가족방으로 복귀
[ ] 진행도 업데이트 확인
```

### 6️⃣ 관리자 페이지 (선택사항)

```
[ ] 로그인 화면 → "관리자 로그인"
[ ] ID: admin, PW: 1234 입력
[ ] 관리자 화면 표시
[ ] 구절 등록 가능
[ ] 교회 정보 관리 가능
[ ] 로고 업로드 테스트 (SVG, 최대 500KB)
```

---

## 🔍 기술 검증

### 캐시 정책 검증

```
F12 개발자 도구 → Network 탭에서 확인

[ ] index.html: Cache-Control 없음 (또는 no-cache)
[ ] manifest.json: Cache-Control no-cache
[ ] icons/*.png: Cache-Control no-cache
[ ] 기타 JS/CSS: Cache-Control max-age=0

확인 방법:
1. 네트워크 탭 → 새로고침
2. 각 파일의 "Response Headers" 확인
3. Cache-Control 값 확인
```

### Service Worker 검증

```
F12 개발자 도구 → Application → Service Workers

[ ] Service Worker 등록됨 (v39)
[ ] 상태: activated and running
[ ] 새로고침 후에도 유지됨

캐시 확인:
1. Application → Cache Storage
2. pat-bible-app-v39 캐시 확인
3. 구 버전 (v38, v37 등) 없음
```

### 권한 검증

```
Settings → Apps → PAT Bible → Permissions

[ ] 마이크: 필요 (음성 인식용)
[ ] 위치: 거부됨 (수집 안 함)
[ ] 연락처: 거부됨 (수집 안 함)
[ ] 카메라: 거부됨 (수집 안 함)
[ ] 저장소: 필요한 경우만 (PWA 설치용)
```

---

## 🚀 성능 테스트

### 앱 시작 시간

```
[ ] 앱 아이콘 터치 ~ 로그인 화면 표시: 3초 이내
[ ] 깜빡임 없음
[ ] 스무스한 애니메이션
```

### 메모리 사용량

```
Settings → Apps → App info → Memory

[ ] 메모리 사용: 100MB 이상 아님 (정상: 50-80MB)
[ ] 장시간 사용 시 메모리 누수 없음
```

### 배터리 소비

```
[ ] 1시간 사용 ~ 배터리 5% 이상 소비 아님 (정상)
[ ] Firebase 폴링으로 인한 과다 사용 없음
```

---

## 📊 데이터 안전 검증

### 로컬 저장소 확인

```
브라우저 버전:
F12 → Application → Local Storage → pat-bible-app.web.app

[ ] pat_family_id: 존재
[ ] pat_admin_logged_in: admin 로그인 시에만
[ ] pat_records: 암송 기록 (암호화되어 있어야 함)
```

### Firebase 저장 확인

```
[ ] 선택적으로만 저장됨 (기본은 로컬)
[ ] 저장된 데이터: 가족방, 미션 기록
[ ] HTTPS 암호화 전송 (Network 탭에서 https:// 확인)
```

### 마이크 데이터

```
[ ] 음성은 로컬에만 저장
[ ] 음성은 네트워크 전송 안 함
[ ] 검증만 수행 후 폐기
```

---

## 🎯 교차 기기 동기화 테스트

### 웹 ↔ 모바일 동기화

```
기기 A (웹): https://pat-bible-app.web.app
기기 B (모바일 앱): TWA 앱

[ ] 기기 A에서 구절 등록 → 기기 B에서 자동 동기화 (10초)
[ ] 기기 B에서 미션 완료 → 기기 A에서 자동 반영 (10초)
[ ] 가족방 공유 정상 작동
[ ] 오류/크래시 없음
```

---

## 🐛 버그 체크

### 일반 버그

```
[ ] 텍스트 깨짐 없음 (한글 정상)
[ ] 버튼 클릭 반응 정상
[ ] 페이지 전환 애니메이션 부드러움
[ ] 팝업 모달 정상 작동
[ ] 입력 필드 포커스 정상
```

### 크래시 테스트

```
[ ] 앱 강제 종료 후 재실행 → 정상 복구
[ ] 네트워크 끊김 상태에서 앱 사용 → 오류 없음
[ ] 권한 거부 시 우아하게 처리
[ ] 로그인 실패 시 명확한 에러 메시지
```

### 모바일 전용 버그

```
[ ] 화면 회전 (세로/가로) → 정상 작동
[ ] 상태바 숨김 모드 정상
[ ] 노치 디스플레이 대응 정상
[ ] 다크 모드에서도 글자 선명함
```

---

## 📋 최종 검수 체크리스트

```
기능성:
[ ] 모든 기능 정상 작동
[ ] 깜빡임 없음
[ ] 크래시 없음

보안:
[ ] 개인정보 로컬 저장 (기본)
[ ] 비밀번호 해싱
[ ] HTTPS 암호화

성능:
[ ] 앱 시작: 3초 이내
[ ] 메모리: 정상 범위
[ ] 배터리: 정상 소비

규정:
[ ] 개인정보처리방침 동의
[ ] 권한 명확히 설명
[ ] 콘텐츠 등급 정확
```

---

## 🎉 테스트 완료 후

1. ✅ 모든 항목 확인 완료
2. 📸 스크린샷 5개 촬영 (로그인, 가족방, 음성, 타이핑, 완료)
3. 📝 Google Play Console 가입
4. 📤 APK 업로드
5. 🔍 검수 신청
6. ⏳ Google 검수 대기 (1-3일)
7. ✨ 출시!

---

**테스트 완료 시 날짜와 결과를 기록하세요!**

마지막 테스트: ________________
테스터: ________________
결과: ✅ 완벽 / ⚠️ 수정 필요 / ❌ 재테스트

