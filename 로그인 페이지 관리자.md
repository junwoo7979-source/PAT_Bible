# 로그인 페이지 관리자 — 작업 기록

작업일: 2026-06-20  
버전: v64  
배포: https://pat-bible-app.web.app

---

## 완료한 작업

### 1. 관리자 탭 전환 버그 수정
- **원인**: `getRankedFamilies()` 함수에서 가족 미로그인 시 `loadFamilyProfile()`이 `null`을 반환하는데, `null.familyId`에 접근해 TypeError 발생
- **수정**: `app/js/admin.js` — `if(!profile) return [];` null 체크 추가
- **결과**: "🏆 시상 관리" 탭 클릭 시 에러 없이 "데이터가 없습니다" 정상 표시

### 2. 교구별 현황 관리자 페이지 추가
- **위치**: 관리자 → 교회 정보 탭 → 교회 정보 관리 섹션 하단
- **내용**: 🧭 교구별 현황 (1교구/2교구/3교구/블레싱 실시간 진도)
- **구현**:
  - `app/index.html`: `adminParishList` div 추가
  - `app/js/memorize.js`: `renderParishStatsFromAggregated()`에서 `adminParishList`도 동시 렌더링
  - `app/js/app-core.js`: `renderAdmin()`에서 `computeAggregatedData()` 호출

### 3. SW 캐시 버전 v64 업데이트
- `app/sw.js`: `pat-bible-app-v63` → `pat-bible-app-v64`

---

## 관리자 로그인 방법
- 로그인 화면 → "🔐 교회 관리자 로그인" 버튼
- 아이디: `admin` / 비밀번호: `1234`

## 관리자 탭 구조
| 탭 | 내용 |
|---|---|
| 📖 구절 등록 | 이번 주 암송 구절 입력 + 미리보기 |
| ⛪ 교회 정보 | 교회 이름/로고 + 🧭 교구별 현황 (신규) |
| 🏆 시상 관리 | 가족 실천율 순위 / 개인별 조회 |
| 🔑 비밀번호 | 가족 비밀번호 초기화 |

---

## 커밋
`0bd95d9` — v64: 관리자 탭 전환 버그 수정 + 교구별 현황 관리자 페이지 추가
